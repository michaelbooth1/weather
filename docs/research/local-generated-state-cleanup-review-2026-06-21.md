# Local Generated State Cleanup Review - 2026-06-21

Source: `data/backtest/local_generated_state_cleanup_report.md`, generated by
`python -m weather.operations.local_generated_state_cleanup`.

## Durable Scratch Findings

The current `scratch/` tree contains 21 non-cache files totaling about
234.8 KiB. They are small enough to keep until active work settles, but they
should not be treated as current operator entrypoints.

The no-market location transfer scratch scripts are superseded by the canonical
package harness:

```powershell
python -m weather.reporting.no_market_location_transfer
```

Their durable conclusion is already reflected in the no-market roadmap work:
extra-location labels require target-specific validation and should not be
blindly pooled into a target market. Keep the scratch JSON/Markdown pairs until
that active work is fully settled, then delete them with the paired scripts.

The 2026-06-18 price-free model scratch report is diagnostic evidence only. Its
durable path is the package scorer and daily-refresh integration owned by item
155, not the scratch CSV/JSON files.

The 2026-06-18 CLOB and hourly-model scratch reports are historical diagnostic
evidence for active promotion and live-forward blocker analysis. They should
remain local until the corresponding active roadmap blockers are resolved or the
findings are copied into the owning item files.

`scratch/toronto_outlier_replay_report.md` is historical replay evidence. It is
not a current command surface and can be deleted after any still-useful finding
is cited from the owning roadmap item.

## Research Tooling

`tools/research/research_harness.py --validate --smoke --include-fixtures` now
passes. The harness inventory intentionally keeps:

- 1 supported harness entrypoint.
- 8 fixture-only local/historical scripts.
- 17 retired scripts or compatibility wrappers.

Retired research scripts should stay only while they are documented harness
sentinels or compatibility wrappers. New ad-hoc scripts must be added to the
harness inventory before they can be kept under `tools/research`.

## Deferred Cleanup

The dry-run found 32 safe cache roots, 985 files, and about 15.2 MiB of local
cache state. Delete those only after active agent work and tests finish:
`__pycache__`, `.pytest_cache`, `.ruff_cache`, and
`src/weather_market.egg-info`.

The line-ending scan found 146 tracked text files with CRLF despite
`.gitattributes` declaring `* text=auto eol=lf`. Normalize those in a dedicated
docs/code hygiene change after dirty files settle.

# Codex Goal Prompt For Market-Making Reward Research

Date: 2026-06-26

Use this prompt when starting a Codex goal for the next preparation pass. It is intentionally repo-specific and evidence-heavy. It should keep the agent researching, auditing, simulating, and writing artifacts instead of jumping to live orders.

Codex usage notes:
- Paste the fenced `Goal:` block into `/goal` so Codex treats the text as both the objective and the completion criteria.
- Keep this repo document as the authoritative prompt source for now. If this becomes a repeated workflow across repos, convert it into a Codex skill instead of relying on deprecated custom prompts.
- The goal is intentionally long. Its main control is not verbosity; it is the combination of safety constraints, repo paths, required commands, pass/warn/block gates, and a clear anti-premature-completion loop.

```text
Goal: prepare this repo for a small-scale market-making test whose long-term objective is farming Polymarket liquidity rewards and maker rebates without taking undocumented live risk.

Operating constraints:
- Do not place live orders.
- Do not load, request, print, or modify private keys, API keys, wallet seed material, or account credentials.
- Do not paste raw provider error URLs or status payloads that contain secret-like query values. If a normal status/report command exposes an unredacted key, fix the redaction boundary before continuing.
- Weather.com provider credentials must remain outside the repo. Data collection may use `WEATHER_COM_API_KEY` or `WEATHER_COM_KEY` from the operator environment; if neither is set, treat the resulting source-status degradation as fail-closed data evidence, not permission to reintroduce a hardcoded key.
- Treat `mm_platform_verification_v0.2` secret-redaction proof as a hard live-pilot gate. The platform artifact must reject exact secret fields and unredacted secret-like URL query values, and must include fresh `secret_redaction` proof for status output, source/docs scans, generated-artifact scans, no unredacted findings, and recorded scan scope.
- Do not start a live pilot.
- Default all work to research, repo audit, shadow runs, paper-live-forward runs, simulations, reports, and narrowly scoped safety fixes.
- If a command could mutate exchange state, stop and document the blocker instead.
- Use current official Polymarket docs before relying on older repo assumptions. Polymarket International and Polymarket US are distinct surfaces.
- Treat all exchange, fee, reward, API, legal, and eligibility assumptions as time-sensitive. Browse and cite official sources at the start of the pass.
- Prefer evidence from this repo over generic market-making advice once local artifacts exist.

Main objective:
Get us as prepared as possible for a future small-scale market-making pilot. The core business goal is to farm liquidity rewards well, but the near-term objective is to prove that the bot can identify reward-eligible markets, quote only when the evidence supports it, avoid toxic flow, survive stale data and account failures, and produce auditable paper evidence.

Success definition for this pass:
- Produce a repo-grounded go/no-go decision for live market making, defaulting to NO-GO unless every required gate passes.
- Identify the smallest market/band/time-window candidate that could later become a min-size pilot, or explain why no candidate exists.
- Separate policy no-edge, stale/missing data, reward-score insufficiency, queue/fill uncertainty, model uncertainty, and operational live-readiness blockers.
- Leave the repo better prepared by improving only research, audit, simulation, reporting, observability, or safety code. Do not optimize for live throughput yet.
- Produce written artifacts that a future Codex goal can pick up without rediscovering the current state.

Execution loop:
- Treat this as a long-running research and readiness goal, not a single-command check.
- After each completed branch of work, choose the next highest-value safe branch from: official-rule research, repo audit, artifact interrogation, shadow run, bounded paper score, readiness report, simulation gap, safety/reporting fix, test verification, or next-action documentation.
- Do not stop just because one command passes, one report is written, or one blocker is found. Continue until every deliverable is updated and the next safe action would require live credentials, live exchange mutation, explicit operator funding/action, or a new user decision.
- When context is compacted or the thread resumes, re-read this prompt, run `git status --short`, inspect the latest readiness JSON/report, inspect `data/mm_runs/daily_roll_status.json`, inspect the latest scenario coverage matrix, and verify the target date before continuing.
- Prefer current repo artifacts over memory. If an artifact is stale, target-date mismatched, diagnostic-only, or post-settlement, label it as such and keep looking for countable active-window evidence.
- Keep a running distinction between facts proven by official docs, facts proven by local artifacts, assumptions, and recommendations.
- If you make a code change, run the narrowest meaningful test set immediately, then update the affected research/readiness document with the command and result.
- If you cannot safely continue, write down the exact blocker, the unsafe action being avoided, and the next user/operator decision needed.
- Treat zero quote legs as absent fill evidence. A paper score with no quote legs may be useful quote-starvation evidence, but it must not count as promotion-grade fill proof merely because there were no fills, missing trade rows, or unresolved resting quotes to audit.

Repo context:
- Work in `C:\Users\micha\Desktop\github\weather`.
- Existing market-making docs live under `docs/research`.
- Core maker modules:
  - `src/weather/market/market_making_run.py`
  - `src/weather/market/mm_policy.py`
  - `src/weather/market/mm_risk.py`
  - `src/weather/market/mm_paper.py`
  - `src/weather/market/mm_paper_scoring.py`
  - `src/weather/market/mm_exchange.py`
  - `src/weather/market/exchange_economics.py`
  - `src/weather/market/clob_recon.py`
  - `src/weather/market/market_microstructure.py`
  - `src/weather/market/info_event_calendar.py`
- Existing docs to read first:
  - `docs/research/MARKET_MAKING_PLAN.md`
  - `docs/research/MARKET_MAKING_RESEARCH_AUDIT_2026-06-13.md`
  - `docs/research/MM_INITIAL_TEST_RUN_DESIGN.md`
  - `docs/research/MM_MODEL_READINESS_GAP_PLAN.md`
  - `docs/research/MM_MODEL_USE_READINESS_2026-06-26.md`
  - `docs/research/MARKET_MAKING_LIVE_RUNBOOK_2026-06-15.md`
  - `docs/research/MARKET_MAKING_PLATFORM_VERIFICATION_2026-06-16.md`
  - `docs/research/MM_REWARD_FARMING_READINESS_2026-06-26.md`
  - `docs/research/MM_REPO_AUDIT_2026-06-26.md`
  - `docs/research/MM_QUOTE_STARVATION_DIAGNOSIS_2026-06-26.md`
  - `docs/research/MM_KNOWN_EDGE_COVERAGE_2026-06-26.md`
  - `docs/research/MM_SIMULATION_RESULTS_2026-06-26.md`
  - `docs/research/MM_SIMULATION_SCENARIO_COVERAGE_2026-06-27.md`
  - `docs/research/MM_SMALL_SCALE_TEST_PLAN_2026-06-26.md`
- Current evidence artifacts to inspect:
  - `data/backtest/mm_paper_report.json`
  - `data/backtest/mm_paper_report.md`
  - `data/backtest/mm_known_edge_map.json`
  - latest `model_variant_bakeoff.json` under `data/mm_runs/<target-date>/<run-id>/`
  - `data/backtest/exchange_economics_snapshot.json`
  - `data/backtest/exchange_economics_accepted_snapshot.json`
  - `data/backtest/exchange_economics_drift_report.json`
  - `data/backtest/event_metadata_validation.json`
  - latest run folder under `data/mm_runs/<target-date>/`
  - latest CLOB loop status files under `data/snapshots`

- Latest current all-market June 27 active-window paper-live-forward one-shot after the CLOB recovery sequence: `data/mm_runs/2026-06-27/20260627T135932865534Z`.
- Latest current all-market bounded paper diagnostic: `data/backtest/mm_paper_paperlive_20260627T135932_after_clob_recovery_source_status_block.json`.
- Latest current all-market target-date-aligned readiness report: `data/backtest/mm_live_readiness_20260627T135932_after_clob_recovery_source_status_block.json`.
- Latest aggregate active-window paper-live-forward diagnostic: `data/backtest/mm_paper_paperlive_20260627_latest5_active_after_clob_recovery_source_status_block.json`, with readiness `data/backtest/mm_live_readiness_20260627_latest5_active_after_clob_recovery_source_status_block.json`. It selected four eligible active-day live-forward folders from the latest-five bounded query (`111731`, `112944`, `131743`, and `135932`) and still found 528 quote rows, 0 quote-permission rows, 0 live permissions, 0 quoted legs, fill evidence `BLOCK` with `no_quote_legs`, reward score 0, and readiness `BLOCK` with 11 blockers.
- Latest repo-grounded simulation coverage matrix: `docs/research/MM_SIMULATION_SCENARIO_COVERAGE_2026-06-27.md`. It maps the requested clean-day, stale-data, event-window, reward, queue/fill, risk, cancel-all, model-use, and daily-roll scenarios to concrete artifacts/tests. Current conclusion remains NO-GO because the latest active-window run and latest-five aggregate both block before quote permission, so quote/fill/reward scenarios are not proven until external source-status repair and a fresh active-window rerun.
- Latest non-mutating runtime/recovery rechecks on `2026-06-27`: official Polymarket US economics drift is still `PASS` via `data/backtest/exchange_economics_drift_20260627_goal_recheck.json`, and current Polymarket.com global/International docs were re-opened only to preserve the US-vs-global distinction. During the `13:43Z` to `13:59Z` pass, active CLOB briefly degraded to zero books/tokens for every market, then recovered after `location_config_refresh`, target-date event validation, fixed-date public CLOB capture, and explicit fixed-date restart. A later `14:13Z` check found duplicate local-date CLOB loops and a second fixed-date restart briefly restored `date_selection = fixed_target_date`. Superseding checks found the running loop had drifted again to `date_selection = market_local_date`, `target_date = null`, price history/WebSocket capture enabled, and a process command line with no `--date`, no `--no-price-history`, and no `--no-websocket-events`; the latest `14:31Z` status has useful rows and fresh books, but per-market `target_date` remains null. The explicit target-date audit after the undated restart returned `ok = true` only under startup-gap grace, so treat it as diagnostic, not promotion-quality fixed-date evidence. The repo now has a focused safeguard: `ensure_clob_loop` preserves a fixed `target_date` from status when restarting, and the operations-dashboard CLOB restart path preserves status-derived target date and enrichment flags; focused CLOB/ops tests passed. Snapshot tracker follow-up status still returned `STALE_CODE`, `runtime_identity_matches_current = false`, 3 dark markets (`dallas`, `denver`, `houston`), snapshot cadence `BLOCK`, early-hour coverage `BLOCK`, and source-status `BLOCK` on `settlement_source_auth_failure` with Weather.com credential env vars absent/redacted. Observation trigger is fresh; daily roll is prior-target/post-settlement/noncountable with `SCHEDULED_WAIT` until `19:30` local.
- The latest readiness report was regenerated after adding non-secret provider-credential diagnostics. It records `source_status_weather_com_credential_present = false`, `source_status_weather_com_credential_present_by_var = {WEATHER_COM_API_KEY: false, WEATHER_COM_KEY: false}`, and `source_status_weather_com_credential_values_redacted = true`; its top source-status actions now start with configuring `WEATHER_COM_API_KEY` or `WEATHER_COM_KEY` outside the repo before rerunning snapshot collection/source-status backfill and keyless shadow/readiness. Upstream `snapshot_tracker --status` now exposes the same boolean-only diagnostic in `source_status_proof_v0.2` at `source_status_proof.summary.provider_credential_environment` and `source_status_proof.summary.weather_com_credential_*`. The live-forward SLO recovery checklist should also surface `settlement_source_auth_failure_missing_weather_com_credentials` with owner `external Weather.com provider credentials` when both env vars are absent. The Streamlit Market Making cockpit now reads latest target-date `mm_live_readiness*.json` and shows this no-go/root-cause evidence directly.
- Latest current all-market active-window paper-forward drill after CLOB recovery: `data/mm_runs/2026-06-27/20260627T135932865534Z`, scored by `data/backtest/mm_paper_paperlive_20260627T135932_after_clob_recovery_source_status_block.json`, with readiness `data/backtest/mm_live_readiness_20260627T135932_after_clob_recovery_source_status_block.json`. It emitted 132 quote-intent rows, 132 no-quote rows, 0 quote-permission rows, 0 live-permission rows, active-day evidence mode, live-forward gate `BLOCK`, `current_counts_toward_live_forward_gate = false`, and readiness `BLOCK` with 11 blockers. First failing gate is `source_status_degradation`; preflight remediation also shows 3 stale model rows and 1 stale/missing snapshot-model row. Paper fill evidence is `BLOCK` with `no_quote_legs`, exchange economics is `PASS`, and actual payout evidence is false. It overlapped an `INFO_EVENT_WU_CURRENT_PRINT` window with event action `widen`, not a live-readiness exception.
- Latest current all-market shadow/operator drill for comparison after CLOB recovery: `data/mm_runs/2026-06-27/20260627T135111048558Z`, scored by `data/backtest/mm_paper_shadow_20260627T135111_after_clob_recovery_source_status_block.json`, with readiness `data/backtest/mm_live_readiness_20260627T135111_after_clob_recovery_source_status_block.json`. It emitted 132 quote-intent rows, 132 no-quote rows, 0 quote-permission rows, and 0 live-permission rows; its first failing gate is still `source_status_degradation`, paper fill evidence is `BLOCK` with `no_quote_legs`, and readiness is `BLOCK` with 11 blockers. It overlapped an `INFO_EVENT_METAR_PRINT` window, so event-gate suppression is contextual and not a live-readiness exception.
- Prior current all-market shadow/operator drill after observation-trigger recovery: `data/mm_runs/2026-06-27/20260627T130826087166Z`, scored by `data/backtest/mm_paper_shadow_20260627T130826_current_source_status_block.json`, with readiness `data/backtest/mm_live_readiness_20260627T130826_current_source_status_block.json`.
- Prior observation-trigger incident drill: `data/mm_runs/2026-06-27/20260627T125739622819Z`, scored by `data/backtest/mm_paper_shadow_20260627T125739_current_source_status_block.json`, with readiness `data/backtest/mm_live_readiness_20260627T125739_current_source_status_block.json`. It is historical because `weather.operations.observation_trigger ensure` later removed a dead writer lock for PID 1536, started a fresh watcher, and the 130826 drill cleared observation-trigger root causes while preserving the source-status no-go.
- Prior all-market source-status-only drill after forced snapshot/model refresh: `data/mm_runs/2026-06-27/20260627T124837800878Z`, scored by `data/backtest/mm_paper_shadow_20260627T124837_current_source_status_block.json`, with readiness `data/backtest/mm_live_readiness_20260627T124837_current_source_status_block.json`.
- Prior current all-market June 27 shadow/operator drill before the forced snapshot/model refresh: `data/mm_runs/2026-06-27/20260627T122613569835Z`, scored by `data/backtest/mm_paper_shadow_20260627T122613_current_source_status_block.json`, with readiness `data/backtest/mm_live_readiness_20260627T122613_current_source_status_block.json`. It is historical because its six stale model rows cleared after `snapshot_tracker --force --market all --date 2026-06-27`.
- Prior current all-market June 27 active-window paper-live-forward one-shot with observation-trigger runtime incidents: `data/mm_runs/2026-06-27/20260627T111731961696Z`, scored by `data/backtest/mm_paper_paperlive_20260627T111731_active_source_status_block.json`, with readiness `data/backtest/mm_live_readiness_20260627T111731_paperlive_active_source_status_block.json`.
- Prior current all-market June 27 shadow/operator drill: `data/mm_runs/2026-06-27/20260627T110334081112Z`, scored by `data/backtest/mm_paper_shadow_20260627T110334_current_source_status_block.json`, with readiness `data/backtest/mm_live_readiness_20260627T110334_current_source_status_block.json`.
- Prior post-backfill all-market source-status probe: `data/mm_runs/2026-06-27/20260627T103532784124Z`, scored by `data/backtest/mm_paper_shadow_20260627T103532_after_source_backfill.json`, with readiness `data/backtest/mm_live_readiness_20260627T103532_after_source_backfill.json`.
- Previous all-market zero-quote shadow `data/mm_runs/2026-06-27/20260627T083841810915Z` is now historical pre-gate context: it had preflight `PASS` even though fleet source-status proof was `BLOCK`; do not treat that preflight result as current readiness evidence.
- Latest nonzero-quote all-market shadow/operator drill remains `data/mm_runs/2026-06-27/20260627T073107208602Z`, with diagnostic paper/readiness artifacts `data/backtest/mm_paper_shadow_20260627T073107_current_all.json` and `data/backtest/mm_live_readiness_20260627T073107_current_all.json`.
- Latest selected-market June 27 subset drill remains historical context: `data/mm_runs/2026-06-27/20260627T071559212462Z`, scored by `data/backtest/mm_paper_shadow_20260627T071559_subset_austin_dallas_houston.json`, with readiness `data/backtest/mm_live_readiness_20260627T071559_subset_austin_dallas_houston.json`.
- Latest known-edge diagnostic remains historical because the 071559 subset score did not emit a new known-edge artifact: `data/backtest/mm_known_edge_shadow_20260627T061148_subset_austin_dallas_houston.json`. Treat generated known-edge maps as diagnostic only until countable active-window markouts exist.
- All-market June 27 no-go diagnostic: `data/mm_runs/2026-06-27/20260627T055820610723Z`, scored by `data/backtest/mm_paper_shadow_20260627T055820_after_metadata_snapshot_refresh.json`, known-edge diagnostic `data/backtest/mm_known_edge_shadow_20260627T055820_after_metadata_snapshot_refresh.json`, and readiness `data/backtest/mm_live_readiness_20260627T055820_after_metadata_snapshot_refresh.json`.
- Historical selected-market candidate drill: `data/mm_runs/2026-06-27/20260627T061148175884Z`, scored by `data/backtest/mm_paper_shadow_20260627T061148_subset_austin_dallas_houston.json`, known-edge diagnostic `data/backtest/mm_known_edge_shadow_20260627T061148_subset_austin_dallas_houston.json`, and readiness `data/backtest/mm_live_readiness_20260627T061148_subset_austin_dallas_houston.json`.
- Latest daily-status readiness diagnostic: `data/backtest/mm_live_readiness_20260627T0642_daily_status_stale_heartbeat.json`. It uses current `data/mm_runs/daily_roll_status.json` and selects a same-target June 26 paper score by default; it remains `BLOCK` because the daily-roll status target is June 26 while the supervisor expected target is June 27, the roll is post-settlement/noncountable, preflight is blocked, and runtime liveness is stale. Its Summary now surfaces `daily_roll_action = blocked_restart_required`, `runtime_root_cause_class = stale_heartbeat_metadata`, `artifact_liveness_status = STALE_HEARTBEAT_METADATA`, `operator_restart_recommended = true`, and `operator_restart_reason = stale_heartbeat_metadata`.
- Latest explicit mismatch diagnostic: `data/backtest/mm_live_readiness_20260627T0625_daily_status_target_mismatch.json`. It passes a June 27 paper score against the June 26 daily status and must remain `BLOCK` on `readiness_inputs_target_date_aligned`.
- Current all-market result to verify, not blindly assume: preflight `BLOCK`, 132 quote-intent rows, 132 no-quote rows, 0 quote-permission rows, 0 live permissions, readiness `BLOCK`, 11 blockers, and `live_capital_permission = false`. Paper score has 0 quoted legs, 0 conservative fills, 0 queue-estimated fill legs, fill evidence `BLOCK` with `no_quote_legs`, total reward score 0, and actual payout evidence false.
- Current all-market preflight blocker to verify: all 12 selected markets fail `source_status_degradation`; the latest 135932 readiness has `evidence_mode = active_day_live_forward`, paper freshness `PASS`, `current_counts_toward_live_forward_gate = false`, `model_freshness_failed_market_count = 3`, and `snapshot_model_source_failing_gate_counts = {model_freshness: 3, source_status_degradation: 12}`. The latest remediation root-cause counts are `source_status_degradation_blocked = 12`, `stale_model_row = 3`, and `stale_or_missing_snapshot_model_rows = 1`. The source-status recovery sequence is still external source-health repair first: if `WEATHER_COM_API_KEY` and `WEATHER_COM_KEY` are absent, configure one outside the repo, then run `python -m weather.collection.snapshot_tracker --backfill-source-status --overwrite-source-status`, then rerun paper-live-forward/readiness. The observation-trigger recovery sequence remains `python -m weather.operations.observation_trigger ensure` if the watcher goes stale again; the current watcher-health recheck is fresh. Top-level reason counts are therefore 132 `NO_QUOTE_MISSING_PREFLIGHT`, not a policy permission split. Before the prior post-backfill run, the source-status backfill rewrote 3,756 rows across the 12 current June 27 folders; fresh `snapshot_tracker --status` still reports `source_status.status = BLOCK`, `root_cause_class = settlement_source_auth_failure`, `settlement_auth_failure_source_count = 12`, `weather_com_credential_present = false`, and `weather_com_credential_values_redacted = true`. The latest paper-live-forward run blocked before policy quote permission could be interpreted.
- Historical selected-subset result to verify when comparing regressions: preflight `PASS`, 33 no-quote rows, 0 quote permissions, 0 live permissions, readiness `BLOCK`, 8 blockers, and `live_capital_permission = false`. Its blocker split was 25 `NO_QUOTE_SNAPSHOT_CADENCE_DEGRADED` and 8 `NO_QUOTE_MISSING_BOOK`.
- Current coverage split to verify: before the source-status preflight gate, Toronto, NYC, Atlanta, Chicago, Miami, Los Angeles, San Francisco, and Seattle were primarily known-edge/promotion blocked, while Austin, Dallas, Houston, and Denver were closer to harvest-only exploration but blocked by selected-snapshot cadence gaps and missing books. The current all-market gate is now earlier and stricter: source-status degradation blocks every market before quote permission can be interpreted. Historical 073107 quote-permission market counts were Austin 4, Dallas 7, Denver 5, and Houston 7, but they are noncountable shadow evidence.
- Current accepted known-edge map to verify: 17 records, 7 `harvest_only`, 3 `edge_research`, and 7 `no_quote`. Do not replace it with any generated diagnostic candidate map without countable active-window markouts and promotion-gate evidence.
- Current status-output hygiene to verify: `snapshot_tracker --status` should redact secret-like URL query values such as `apiKey`, including empty `apiKey=` values; Weather.com keys are environment-only; the latest captured verifier found 0 raw secret-query parameters and 48 redacted secret-query parameters in stdout. For settlement auth failures, status output should expose `source_status_proof.schema_version = source_status_proof_v0.2`, `source_status_proof.summary.provider_credential_environment`, and `source_status_proof.summary.weather_com_credential_*` with booleans only. The schema registry strict audit should remain clean for the market-making schemas `mm_live_readiness_v0.2`, `mm_quote_blocker_diagnostics_v0.8`, `mm_quote_intent_v0.3`, and `mm_reward_score_diagnostics_v0.2`. The runtime `data/backtest/mm_platform_verification.json` is now a v0.2 fail-safe template; current readiness still blocks because all operator/platform proofs are false and all five `secret_redaction_*` proof checks are missing.

Local operating hypothesis to verify:
- The repo currently points market-making economics at Polymarket US via `data/backtest/exchange_economics_snapshot.json`.
- Older docs and some code paths still discuss Polymarket International. Do not merge US and International rules casually.
- Treat the current weather-maker objective as liquidity-reward/rebate harvesting with strict adverse-selection controls, not directional betting.
- Treat the model as a quote veto, band selector, and research signal until positive, countable paper markouts prove stronger use.
- Treat `docs/research/MM_MODEL_USE_READINESS_2026-06-26.md` as the current model-use gate. Recompute its artifact counts before relying on them, but preserve its default stance: no model-skewed live quoting without countable active-day evidence, complete fills, positive markouts, locked policy, and known-edge permission.

Required research:
1. Read current official Polymarket International docs for liquidity rewards, fees, maker rebates, order lifecycle, order creation, cancel-all, market/user WebSockets, tick size, CLOB market info, and authentication.
2. Read current official Polymarket US docs for incentives/liquidity, fees, order API, authentication, WebSockets, rate limits, and market integrity.
3. Compare International versus US reward scoring and API semantics. Explicitly state which platform the local snapshot claims.
4. Pull in market-making literature only where it changes what we should audit or simulate:
   - inventory-skew/reservation-price models,
   - queue-position value,
   - adverse-selection markouts,
   - liquidity-mining incentives and rebate farming risks.
5. Capture citations in any report you write. Use links, source dates where visible, and note when a rule may have changed.

Required repo audit:
1. Run `git status --short` first. Do not revert unrelated user changes.
2. Inspect the maker modules and tests before proposing changes.
3. Verify that `mm_policy.py` remains pure and has no private-key dependency.
4. Verify that `market_making_run.py` is fail-closed on preflight and cannot emit live-trade permission outside `live-pilot`.
5. Verify that `mm_risk.py` enforces per-band, per-event, correlated-regime, daily drawdown, balance, manual pause, heartbeat, and cancel-all concepts.
6. Verify that `mm_exchange.py` builds post-only/non-taking plans and keeps signing external/injected.
7. Verify that `mm_paper.py` and `mm_paper_scoring.py` score conservative fills separately from queue-estimated fills, and that promotion does not rely only on optimistic queue simulation.
8. Verify that `exchange_economics.py` blocks stale/mismatched economics snapshots and requires rescore when material economics drift.
9. Verify that `clob_recon.py` measures reward competition, executable depth, and markouts rather than assuming rewards are free.

Required artifact interrogation:
1. Identify the current target date from `weather.operations.market_making_daily_roll status`, not from wall-clock intuition alone.
2. Inspect the latest run folder under `data/mm_runs/<target-date>/` and read at least:
   - `run_summary.json`
   - `preflight.json`
   - `quote_intents_long.csv`
   - any lifecycle, budget, useful-work, reward, or report artifacts present in that folder.
3. Break quote results down by market, band, side, action, emitted reason, known-edge permission, known-edge reason, promotion state, event gate, book freshness, CLOB token id, expected reward score, quote risk, and live-trade permission.
4. For no-quote rows, classify them into:
   - expected policy abstention,
   - missing known-edge coverage,
   - promotion block,
   - missing or stale CLOB book,
   - snapshot/source/cadence problem,
   - information-event quote-pull gate,
   - reward metadata or economics problem,
   - risk/budget suppression.
5. For quote rows, inspect bid, ask, size, risk, reward score, rebate value, book spread, book depth, event gate, known-edge state, and whether the row counts toward live-forward evidence.
6. Read `data/backtest/mm_known_edge_map.json` and compare its cells against the latest quote tape. Missing cells should not be silently treated as edge.
7. Read `data/backtest/mm_paper_report.json` and decide whether it covers the current active-day evidence. If it does not, say so explicitly.
8. If full `weather.market.mm_paper` is too slow, run a bounded one-run score with `--run-folder` and document that the result is diagnostic, not a replacement for full-corpus scoring.
9. Inspect exchange-economics drift and event-metadata validation before interpreting reward or fee numbers.
10. Check for stale runtime identities in supervisor statuses before trusting loop liveness.
11. Check CLOB `date_selection`, `target_date`, current process command line, and useful-row counters before treating a strict audit as evidence. If the loop is in `market_local_date` mode for target-date research, or if audit success depends only on startup-gap grace after an undated restart, label it diagnostic and restart with an explicit `--date`.
12. Check `daily_roll_supervisor.start_time_gate`: if the status still points at a prior target date and the gate is waiting for `19:30`, the default scheduler is only a last-window backstop. For full-window reward-farming paper evidence, stop the old paper loop and start the current target date at or after `07:00` local.
13. Check readiness target-date alignment before interpreting quote counts. `readiness_inputs_target_date_aligned` must pass; otherwise paper quote permissions may belong to a different target date than the daily-roll status or latest selected run.
14. Check `daily_roll_target_date_current`: it must pass before treating daily-roll evidence as current. A same-target June 26 paper score is still not current June 27 evidence if the supervisor expected target is June 27.

Required commands:
1. Run focused market-making tests:
   `.\venv\Scripts\python.exe -m pytest tests\market\test_mm_policy.py tests\market\test_mm_risk.py tests\market\test_mm_paper.py tests\market\test_market_making_run.py tests\market\test_mm_exchange.py -q`
2. Run CLOB status:
   `.\venv\Scripts\python.exe -m weather.market.market_microstructure status`
3. Run strict CLOB audit:
   `.\venv\Scripts\python.exe -m weather.market.market_microstructure audit --strict`
4. Run maker daily-roll status:
   `.\venv\Scripts\python.exe -m weather.operations.market_making_daily_roll status`
5. Run one keyless shadow tick for the current target date:
   `.\venv\Scripts\python.exe -m weather.market.market_making_run --date YYYY-MM-DD --budget-usdc 500 --mode shadow --markets all --once`
6. For active-window paper collection, use the managed paper loop only after the local evidence window starts:
   `.\venv\Scripts\python.exe -m weather.operations.market_making_daily_roll stop --date <OLD_TARGET_DATE>`
   then
   `.\venv\Scripts\python.exe -m weather.operations.market_making_daily_roll start --date YYYY-MM-DD --budget-usdc 500 --mode paper-live-forward --markets austin,dallas,houston --force`
7. Do not run `live-pilot` or any command that can place/cancel real orders unless the user explicitly authorizes it in a later turn and all live gates pass.

Optional safe commands when useful:
- Refresh same-day location/event metadata only through the existing safe path:
  `.\venv\Scripts\python.exe -m weather.operations.location_config_refresh --locations config\locations.json --event-metadata config\location_market_events.json`
- Validate event metadata for the target date:
  `.\venv\Scripts\python.exe -m weather.operations.event_metadata_validation --target-date YYYY-MM-DD --markets all`
- Restart the public CLOB loop in fixed-date mode before collecting target-date evidence if status drifted to `market_local_date`:
  `.\venv\Scripts\python.exe -m weather.market.market_microstructure restart --market all --date YYYY-MM-DD --no-price-history --no-websocket-events`
- Publish and accept exchange economics only when official docs have been rechecked and the target date/platform are explicit:
  `.\venv\Scripts\python.exe -m weather.market.exchange_economics publish --target-date YYYY-MM-DD --platform polymarket_us --accept`
- Run a one-run diagnostic paper score when full scoring is too slow:
  `.\venv\Scripts\python.exe -m weather.market.mm_paper --run-folder data\mm_runs\YYYY-MM-DD\RUN_FOLDER --json-out data\backtest\mm_paper_<label>.json --report-out data\backtest\mm_paper_<label>.md --fills-out data\backtest\mm_paper_<label>_fills.csv --known-edge-out data\backtest\mm_known_edge_<label>.json --known-edge-report-out data\backtest\mm_known_edge_<label>.md`
- Run a bounded diagnostic paper score without overwriting the standard report:
  `.\venv\Scripts\python.exe -m weather.market.mm_paper --target-date YYYY-MM-DD --evidence-mode post_settlement_evaluation --latest-n 1 --json-out data\backtest\mm_paper_bounded_<label>.json --report-out data\backtest\mm_paper_bounded_<label>.md --fills-out data\backtest\mm_paper_bounded_<label>_fills.csv --known-edge-out data\backtest\mm_known_edge_bounded_<label>.json --known-edge-report-out data\backtest\mm_known_edge_bounded_<label>.md`
- For faster operational reward/readiness diagnostics, add `--skip-model-variants` to the bounded paper-score command. Treat the resulting report as diagnostic only; it cannot support model-variant promotion.
- For full-corpus quote/no-quote and reward-score diagnostics when fill simulation is too slow, use `--skip-fill-simulation --skip-model-variants` with separate output filenames. Treat the resulting report and known-edge map as summary-only diagnostics; they cannot support fill evidence, P&L, known-edge promotion, or model-variant promotion.
- Re-run the focused test set after any code change:
  `.\venv\Scripts\python.exe -m pytest tests\market\test_mm_policy.py tests\market\test_mm_risk.py tests\market\test_mm_paper.py tests\market\test_market_making_run.py tests\market\test_mm_exchange.py tests\operations\test_runtime_identity.py -q`

Required simulations and analyses:
1. Reward-score simulation:
   - Compute score by side under the active platform formula.
   - Include target-size qualification, discount factor, ticks from best price, order size, expected competition, and zero-score cases.
   - Report reward score separately from expected reward dollars when the reward pool or pro-rata share is uncertain.
2. Rebate and fee simulation:
   - Compare maker rebate value against expected flattening/taker fees and adverse-selection markouts.
   - Treat rebates as paid only on executed maker flow, not on resting quotes.
3. Queue and fill simulation:
   - Keep conservative fills separate from queue-estimated fills.
   - Do not promote based on queue estimates while trade-size or book evidence is incomplete.
4. Adverse-selection simulation:
   - Measure markouts around observation windows, METAR/source-update windows, and high-information event gates.
   - Identify whether a reward-positive quote is still negative after 5m/30m/settlement markouts.
5. Inventory/risk simulation:
   - Stress one-sided fills, correlated weather-city exposure, near-binary markets, stale books, and forced flattening.
   - Check per-band, per-event, day-level, and correlated-regime caps.
6. Model-use simulation:
   - Compare current model fair probabilities, market mids, known-edge map state, and promotion state.
   - Decide whether the model should quote, harvest only, veto, or only collect shadow evidence for each cell.
7. Operational-failure simulation:
   - Verify fail-closed behavior for stale snapshots, stale WU/source rows, stale CLOB books, stale event metadata, stale economics, runtime drift, heartbeat loss, user-stream failure, and cancel-all failure.

Known current state from the 2026-06-26 preparation pass:
- Focused v0.3 policy/orchestrator tests passed with 60 tests plus 5 subtests, and the broader maker/risk/exchange/operations suite passed with 127 tests plus 5 subtests.
- CLOB strict audit passed for 12 markets at the time checked.
- Runtime/snapshot guard tests passed after fixing `snapshot_store.py` to compare scoped process identity using `current_identity_for(process_identity)`.
- Maker useful-work runtime identity now also compares loop statuses against their recorded scoped identity, and `current_identity_for(recorded)` honors `recorded["repo_root"]`. Regression tests passed.
- Active daily-roll target at the latest check was `2026-06-26`, mode `paper-live-forward`, evidence mode `active_day_live_forward`.
- Active-date event metadata validation passed for `2026-06-26`.
- Active-date Polymarket US exchange economics passed and accepted snapshot `xecon-036874d19e56c76f`; `data/backtest/exchange_economics_drift.json` passed with no rescore required.
- Official Polymarket US docs were rechecked again on 2026-06-26, with a later same-day continuation recheck finding no actionable drift from accepted snapshot `xecon-036874d19e56c76f`. The accepted economics snapshot still matches the core US fee/reward assumptions, but economics proof is not live API proof: US order entry uses `participateDontInitiate`, cancel-all requires zero-open-order confirmation, private WebSocket order reconciliation is mandatory for final state, and 5-second latency-stopgap rejects need explicit handling before live-pilot scale. The US fee page also advertises a temporary high-volume taker rebate; do not count it in small-scale economics unless account eligibility, qualifying volume, payout terms, and effective dates are proven. The adapter now classifies documented US latency-stopgap order rejects as no-acceptance stale-price protection requiring book refresh/recompute, and treats the same response on pure cancels as a live-readiness blocker.
- Polymarket US private WebSocket order-update fixtures now normalize into lifecycle/fill rows for offline reconciliation tests. This covers partial/full fills, cancels, rejects, expirations, and replacements, but does not prove real private-stream connectivity.
- MM-2 cancel-all probe status now requires structured evidence of a cancel-all request plus zero open orders afterward. A generic canceled user-stream event does not prove cancel-all readiness.
- The live-pilot platform-verification gate now requires `mm_platform_verification_v0.2`; v0.1 or boolean-only artifacts should fail closed until they include maker-only order field proof, private-stream lifecycle/fill/final-state reconciliation, cancel-all zero-open-order proof, and US latency-stopgap proof.
- Current standard full-corpus paper report `data/backtest/mm_paper_full_standard_model_variants_release_quotes_20260626.json` has 636,005 quote rows, 71,836 quote legs, 44 conservative fills, 13,045 queue-estimated fill legs, 2 live-forward paper days, fill evidence `BLOCK`, and net paper P&L after fees/incentives of 2.641678 USDC.
- Paper scoring freshness is PASS, but fill evidence completeness is BLOCK because trade-size and queue/book evidence is incomplete.
- Current accepted known-edge map has 17 records: 7 `harvest_only`, 3 `edge_research`, and 7 `no_quote`. Older generated or diagnostic maps may show larger counts, such as 238 cells, but they are not the accepted permission surface unless promotion-grade evidence proves the change.
- Model variant bakeoff is PASS as an experiment but promotion remains BLOCK.
- Latest active-date safe shadow tick for 2026-06-25 emitted 132 no-quote rows, 0 quote permissions, and 0 live-trade permissions with preflight PASS. First failing gate was `policy`; root cause class was `policy_no_edge`; reason counts were `NO_QUOTE_KNOWN_EDGE_PERMISSION = 121`, `NO_QUOTE_MISSING_BOOK = 10`, and `NO_QUOTE_SNAPSHOT_CADENCE_DEGRADED = 1`.
- Latest stable post-settlement one-shot after supervisor restart was `data/mm_runs/2026-06-25/20260626T020148684548Z`: preflight PASS, 1 quote-permission row, 0 live permissions, 121 `NO_QUOTE_KNOWN_EDGE_PERMISSION`, 10 `NO_QUOTE_MISSING_BOOK`, and 1 `QUOTE_HARVEST_MID` Dallas `92-93 F` harvest quote with expected reward score 1.0. Useful-work liveness was SKIPPED because it was post-settlement evidence.
- Known-edge coverage diagnosis is in `docs/research/MM_KNOWN_EDGE_COVERAGE_2026-06-26.md`: the latest current-source active daily-roll diagnostic is dominated by active rows with missing taxonomy/matcher dimensions and `regime = none` / unknown book buckets. Promotion blocks cover Miami, San Francisco, Chicago, Los Angeles, NYC, and Seattle; Toronto, Atlanta, Houston, Austin, Denver, and Dallas require countable markouts before any map change. No current-source active daily-roll row has quote permission.
- A one-run `mm_paper --run-folder data\mm_runs\2026-06-25\20260626T020148684548Z` diagnostic completed in 4.4 seconds, but full-corpus promotion-grade `weather.market.mm_paper` timed out after 300 seconds. Full fill/queue/markout scoring needs bounded, cached, or incremental mode before it can be relied on operationally.
- Bounded `mm_paper` selection now supports `--target-date` / `--run-target-date`, `--evidence-mode`, and `--latest-n`. Bounded reports record `diagnostic_selection_not_full_corpus` so they are not mistaken for full-corpus promotion evidence.
- Bounded smoke run `data/backtest/mm_paper_bounded_latest_postsettlement_20260626.json` selected `data/mm_runs/2026-06-25/20260626T015632370043Z`: 4,488 quote rows, 52 quote legs, 5 conservative fills, 0 queue-estimated fill legs, paper score freshness `NO_ACTIVE_DAY`, fill evidence `BLOCK`, net P&L after fees/incentives -0.00974 USDC, reward estimate 0.
- Reward-score diagnostics now separate Polymarket US score/share from reward-dollar P&L. The same bounded smoke report had 26 quote-permission rows, 52 positive-score legs, total reward score 141.7, score/target-size 0.01417, counterfactual reward 586.263964 USDC under campaign-pool 1000 and competitor-score 100 assumptions, and no actual payout evidence.
- A skip-model-variants bounded smoke report wrote `data/backtest/mm_paper_bounded_latest_postsettlement_skip_variants_20260626.json` in 3.9 seconds at smoke-check time. It selected the same growing post-settlement run, reported 5,148 quote rows, 62 quote legs, 7 conservative fills, model-variant scoring `SKIPPED (skip_model_variants)`, freshness `NO_ACTIVE_DAY`, fill evidence `BLOCK`, net P&L after fees/incentives -0.013636 USDC, reward score 168.95, and no actual reward payout evidence.
- A full-corpus summary-only report wrote `data/backtest/mm_paper_full_summary_only_20260626.json` in about 176 seconds with 36 included run folders, 628,481 quote rows, 71,828 quote legs, 35,914 quote-permission rows, reward score 165,800.676275, counterfactual reward 999.39723 USDC, paper freshness `PASS`, fill evidence `SKIPPED`, and model-variant scoring `SKIPPED`.
- `mm_paper_scoring.py` caches per-token timestamp indexes, streams large casebook inputs, drops full quote-row references from compact quote legs, and releases the full quote-row corpus before fill simulation. Full-corpus standard scoring now completes on current artifacts, but promotion remains blocked by incomplete fill evidence and model-variant promotion gates.
- Bounded latest active-day promotion-grade scoring wrote `data/backtest/mm_paper_bounded_latest_active_skip_variants_20260626.json` in 2.2 seconds after quote-blocker diagnostics were added. It selected `data/mm_runs/2026-06-25/20260626T015448206993Z`, found 132 quote rows, 0 quote legs, 0 quote-permission rows, 121 `NO_QUOTE_KNOWN_EDGE_PERMISSION`, 11 `NO_QUOTE_INFORMATION_EVENT`, reward score 0, freshness `PASS`, and pre-fix fill evidence `PASS` only because no quotes existed. Under the current scorer this zero-quote shape should be treated as `no_quote_legs`, not promotion proof.
- Quote-blocker diagnostics now show the historical pre-v0.3 active-day diagnostic artifact `data/backtest/mm_paper_active_latest_20260626_quote_diag.json` as 4,224 blocked rows, 3,916 known-edge permission-blocked rows, 308 stale-input rows, 44 event-gate-suppressed rows, 0 quote-permission rows, 0 quote uptime, and `mm_quote_blocker_diagnostics_v0.7`. That active-loop tape was produced before `mm_quote_intent_v0.3`, so exact match columns were absent there; the diagnostic-only inferred table surfaced likely active buckets, but a dry-run comparison against the then-current 238-record known-edge map found 0 inferred record matches and 2,112 inferred missing-record rows still missing. The current accepted map has 17 records. The prior accumulated v0.8 active daily-roll score `data/backtest/mm_paper_active_20260626T180437676386Z_v08_current.json` had 31,284 quote rows, 0 quote legs, 29,172 known-edge blockers, 2,112 stale-input blockers, 9,416 event-gate-suppressed rows, 0 inferred known-edge record matches, and 15,642 inferred misses before that folder was quarantined. The latest active daily-roll backoff-recheck score `data/backtest/mm_paper_active_20260626T231738340378Z_v08_backoff_recheck.json` has 4,807 quote rows, 0 quote legs, 4,345 known-edge blockers, 462 stale-input blockers, 1,243 event-gate-suppressed rows, 0 inferred known-edge record matches, and 2,376 inferred misses. The v0.8 report canonicalizes known-edge map hours such as `17:00Z` to `17`, adds diagnostic-only action items, and now renders diagnostic-only required-action buckets; this did not change quote permission.
- Current daily-roll artifact/activity liveness now filters by expected mode and evidence mode so newer one-shot shadow folders do not mask the active paper-live-forward folder, and current live-forward gate reporting reads the latest folder's `live_forward_gate.json`. After the `2026-06-27T03:18:39Z` backoff cleared, safe `ensure --force` quarantined the stale folder and restarted the loop onto current source, but the latest status is no longer countable: status `idle_process`, action `blocked_restart_required`, PID 29180, latest folder `data/mm_runs/2026-06-26/20260627T031938117215Z`, evidence mode `post_settlement_evaluation`, useful-work liveness `SKIPPED`, live-forward gate `BLOCK`, target date `2026-06-26`, expected target date `2026-06-27`, and artifact liveness `STALE_HEARTBEAT_METADATA`. The current operational blocker is stale prior-target post-settlement noncountability; fill-evidence blockage remains a scored-readiness blocker.
- Daily-roll `operator_report` now surfaces supervisor recovery state directly. It exposed `STALE_CODE`/`backoff` before the retry window and later surfaced guarded recovery at the same operator-report boundary; current status has moved on to `blocked_restart_required` / `STALE_HEARTBEAT_METADATA`, so operators do not need to inspect nested supervisor payloads to see the current recovery state.
- Current-source keyless shadow check during `INFO_EVENT_METAR_PRINT`: `data/mm_runs/2026-06-26/20260627T004704070519Z` used current source after the summary-diagnostic edits. It passed preflight across all 12 markets with fresh source/CLOB rows, wrote 132 quote-intent rows, 0 quote-permission rows, and 0 live-trade-permission rows. Reason split: 88 `NO_QUOTE_KNOWN_EDGE_PERMISSION` and 44 `NO_QUOTE_INFORMATION_EVENT`; paper diagnostic `data/backtest/mm_paper_shadow_20260627T004704070519Z_current_source.json` shows 88 known-edge blockers, 132 event-gate-suppressed rows during `INFO_EVENT_METAR_PRINT`, 0 stale-input blockers, 0 inferred known-edge record matches, 11 inferred misses, exchange economics `PASS`, paper freshness `NO_ACTIVE_DAY`, fill evidence `SKIPPED`, and reward score 0.
- Latest current-source keyless shadow check after the event window cleared: `data/mm_runs/2026-06-26/20260627T010734537264Z` passed preflight across all 12 markets and wrote 132 quote-intent rows, 6 harvest-only quote-permission rows, 0 live-trade-permission rows, 88 `NO_QUOTE_KNOWN_EDGE_PERMISSION`, 34 `NO_QUOTE_MISSING_BOOK`, 4 `NO_QUOTE_SNAPSHOT_CADENCE_DEGRADED`, and 6 `QUOTE_HARVEST_MID` rows. The six paper quote permissions were Austin `94-95 F`, Austin `96-97 F`, Dallas `94-95 F`, Dallas `96-97 F`, Houston `92-93 F`, and Houston `94-95 F`. Its diagnostic `data/backtest/mm_paper_shadow_20260627T010734537264Z_current_source.json` has 12 quoted legs, 0 conservative fills, 0 queue-estimated fill legs, reward score 26.8405, counterfactual reward 211.60828 USDC under the default 1000 USDC pool / 100 competitor-score assumption, exchange economics `PASS`, paper freshness `NO_ACTIVE_DAY`, and fill evidence `BLOCK` due to missing trade-size rows, missing queue-book rows, and 12 unresolved resting quotes. This is useful noncountable shadow evidence, not live-forward promotion evidence.
- Pre-recovery fixed score for the paper-live-forward folder with current scoring code: `data/backtest/mm_paper_postsettlement_latest_20260627T020932_current_source.json` was generated at `2026-06-27T02:10:06Z` from `data/mm_runs/2026-06-26/20260627T011838375104Z`. It has 5,016 quote rows, 118 quote-permission rows, 0 live-trade-permission rows, 236 quoted legs, 0 conservative fills, 6 queue-estimated fill legs, reward score 625.5215, counterfactual reward 862.168109 USDC under the default pool/competition assumption, exchange economics `PASS`, paper freshness `NO_ACTIVE_DAY`, fill evidence `BLOCK`, 7,084 missing-size trade rows, 50 missing-book queue legs, and 236 unresolved resting quotes. This is useful current-source scoring evidence, but still post-settlement/noncountable and from a stale-runtime folder.
- Latest regenerated recovered fixed score for the paper-live-forward folder with current scoring code: `data/backtest/mm_paper_postsettlement_recovered_20260627T0233_competitor_source.json` was generated from `data/mm_runs/2026-06-26/20260627T021842583677Z`. It has 1,320 quote rows, 17 quote-permission rows, 0 live-trade-permission rows, 34 quoted legs, 0 conservative fills, 2 queue-estimated fill legs, reward score 89.72025, counterfactual reward 444.480401 USDC, exchange economics `PASS`, paper freshness `NO_ACTIVE_DAY`, fill evidence `BLOCK`, 4,148 missing-size trade rows, 1 missing-book queue leg, and 34 unresolved resting quotes. The counterfactual reward share uses CLOB recon competitor score 112.133982 from 19,019 book rows and 946 slices. The markdown report exposes `Quote permissions / live permissions`, competitor-score source, and CLOB recon rows in the summary, and the JSON summary exposes flat reward-score and fill-evidence fields. This confirms current scoring code can replay the recovered folder and improves reward-share evidence, but it is still post-settlement/noncountable.
- Machine-readable live-readiness summary now exists: run `.\venv\Scripts\python.exe -m weather.market.market_making_readiness --json-out data\backtest\mm_live_readiness_<timestamp>.json --report-out data\backtest\mm_live_readiness_<timestamp>.md`. Latest selected-subset artifact `data/backtest/mm_live_readiness_20260627T071559_subset_austin_dallas_houston.json` is `BLOCK` with `blocker_count = 8`, `live_capital_permission = false`, and `requires_explicit_operator_approval = true`; inspect its Summary table and sorted `next_actions` because the moving paper loop can change preflight, runtime identity, and latest-tick counts. It verifies that the Austin/Dallas/Houston subset has target-date aligned inputs, 0 live permissions, and preflight `PASS`, but it is a one-shot shadow drill with 0 quote permissions, 0 positive P&L, missing actual reward payout, missing live-readiness JSON, and fail-safe v0.2 platform verification missing operator/API and `secret_redaction_*` proofs. Its Summary now exposes `latest_tick_reason_counts = {NO_QUOTE_SNAPSHOT_CADENCE_DEGRADED: 25, NO_QUOTE_MISSING_BOOK: 8}`. Fill evidence is `PASS` only because no quotes rested in this diagnostic. Current top `next_actions` prioritize countable active-window paper-live-forward evidence, live-forward gate, countable quote-permission diagnosis, 14-day evidence, P&L, payout, and operator/platform proof.
- Latest daily-roll status after the guarded window recovered and later went stale: PID 29180 still points at `data/mm_runs/2026-06-26/20260627T031938117215Z`, evidence mode `post_settlement_evaluation`, target date `2026-06-26`, useful-work liveness `SKIPPED`, live-forward gate `BLOCK`, supervisor state `SCHEDULED_WAIT`, action `blocked_restart_required`, root cause `stale_heartbeat_metadata`, and artifact liveness `STALE_HEARTBEAT_METADATA` because `run_summary.json` is stale. It is not countable June 27 active-day evidence, and it should not be treated as current daily-roll evidence until the old loop is stopped/restarted during the intended active evidence window.
- Latest safe market-data checks: the second fixed-date CLOB recovery at `14:15Z` to `14:17Z` briefly restored `date_selection = fixed_target_date`, but superseding status/process checks found the running loop back in undated `market_local_date` mode with `target_date = null`. The latest status has useful rows and fresh books, but the rows are still from an undated loop. `market_microstructure capture`, `raw-refresh`, `audit`, plus managed `loop` / `start-detached` / `restart` / `ensure` now support `--date YYYY-MM-DD`; fixed-date loop health records per-market event dates and degrades on target-date mismatch. New safeguards preserve fixed `target_date` through `ensure` restarts and the operations-dashboard CLOB restart path. `snapshot_tracker` supports explicit `--date/--target-date` plus `--market`, but the latest follow-up status still reports `STALE_CODE` and dark/stale markets, so snapshot loop identity/cadence remains a current operational blocker. CLOB evidence is not countable again until an explicit fixed-date restart produces useful rows and strict audit passes without relying only on startup-grace diagnostics.
- CLOB drift note: earlier in the June 27 pass, the managed loop reported `date_selection = market_local_date` and explicit target-date audit exposed historical gaps. Fixed-date public capture and explicit restart temporarily repaired it, but a later status/process check found local-date drift again. Treat this as current until a fixed-date restart produces useful target-date rows and a strict audit passes without relying only on startup-grace diagnostics.
- Latest exchange-economics drift check at `2026-06-27T12:25:29Z` wrote `data/backtest/exchange_economics_drift_20260627_latest.json` with `status = PASS`, `material_change_count = 0`, `rescore_required = false`, and snapshot `xecon-036874d19e56c76f`. The accepted snapshot still records source check `2026-06-27T04:07:37Z`, platform `polymarket_us`, fee theta `0.05`, maker rebate pool share `0.25`, Climate discount factor `0.30`, and target size `10,000`.
- Follow-up all-market shadow `data/mm_runs/2026-06-27/20260627T075907746405Z` passed preflight but emitted 132 no-quote rows, 0 quote-permission rows, and 0 live-trade-permission rows: 88 `NO_QUOTE_KNOWN_EDGE_PERMISSION`, 35 `NO_QUOTE_SNAPSHOT_CADENCE_DEGRADED`, and 9 `NO_QUOTE_MISSING_BOOK`. Score `data/backtest/mm_paper_shadow_20260627T075907_current_all.json` has 0 quoted legs and reward score 0; readiness `data/backtest/mm_live_readiness_20260627T075907_current_all.json` is `BLOCK` with 8 blockers.
- Source-status degradation is now an explicit preflight and readiness source-data gate. The latest current all-market active-window paper-live-forward one-shot `data/mm_runs/2026-06-27/20260627T135932865534Z` fails closed with preflight `BLOCK`, first failing gate `source_status_degradation`, 132 quote-intent rows, 132 no-quote rows, 132 `NO_QUOTE_MISSING_PREFLIGHT` rows, 0 quote-permission rows, and 0 live-permission rows. `preflight_remediation.json` reports `source_status_degradation_blocked = 12`, `stale_model_row = 3`, and `stale_or_missing_snapshot_model_rows = 1`. The earlier source-status backfill rewrote 3,756 rows from replay inputs across the 12 current June 27 folders, but fresh fleet proof still reports `source_status.status = BLOCK`, `root_cause_class = settlement_source_auth_failure`, `settlement_auth_failure_source_count = 12`, `weather_com_credential_present = false`, and redacted credential values. Its score `data/backtest/mm_paper_paperlive_20260627T135932_after_clob_recovery_source_status_block.json` has 0 quoted legs, fill evidence `BLOCK`, reward score 0, exchange economics `PASS`, paper-day collection gate `OPEN`, paper freshness `PASS`, and `live_capital_gate_status = NOT_EVALUATED_BY_MM_PAPER`. Matching readiness `data/backtest/mm_live_readiness_20260627T135932_after_clob_recovery_source_status_block.json` is `BLOCK` with 11 blockers and `live_capital_permission = false`; its Summary renders `evidence_mode = active_day_live_forward`, `current_counts_toward_live_forward_gate = false`, `snapshot_model_source_failing_gate_counts = {model_freshness: 3, source_status_degradation: 12}`, `source_status_degradation_failed_markets = all 12 selected markets`, `source_status_weather_com_credential_present = false`, `paper_score_live_capital_gate_status = NOT_EVALUATED_BY_MM_PAPER`, `latest_tick_quote_rows = 132`, `latest_tick_no_quote_rows = 132`, and `latest_tick_quote_permission_rows = 0`.
- Bounded latest-five active-window paper-live-forward scoring confirms the no-go is not a one-tick artifact. `data/backtest/mm_paper_paperlive_20260627_latest5_active_after_clob_recovery_source_status_block.json` selected four eligible active-day live-forward folders (`111731`, `112944`, `131743`, `135932`) from 84 discovered folders and 4 candidate folders. It found 528 quote rows, 0 quote-permission rows, 0 live permissions, 0 quoted legs, 0 conservative fills, 0 queue-estimated fill legs, fill evidence `BLOCK` with `no_quote_legs`, paper freshness `PASS`, exchange economics `PASS`, total reward score 0, and no actual payout evidence. Matching readiness `data/backtest/mm_live_readiness_20260627_latest5_active_after_clob_recovery_source_status_block.json` is also `BLOCK` with 11 blockers and `live_capital_permission = false`.
- Current official Polymarket US docs were rechecked again. The US Cancel Multiple Orders page now documents `POST /v1/orders/batched/cancel` with a 20-order cap; `canceledOrderIds` is only an echo/submission record, not final confirmation. The repo now surfaces `max_cancel_order_batch_size = 20` in `mm_exchange.adapter_capability_matrix("polymarket_us")` and a live-readiness note that future batch-cancel paths must chunk requests and reconcile final state from the private order stream.
- Requirement-by-requirement completion/gap audit now exists at `docs/research/MM_GOAL_COMPLETION_AUDIT_2026-06-26.md`. Use it with `data/backtest/mm_live_readiness_20260627T071559_subset_austin_dallas_houston.json.next_actions` before deciding whether the active goal is complete. Current audit verdict is still `NO LIVE CAPITAL`.
- Model-use readiness now exists at `docs/research/MM_MODEL_USE_READINESS_2026-06-26.md`. Latest accepted known-edge map has 17 records: 7 `harvest_only`, 3 `edge_research`, and 7 `no_quote`. Latest model-variant bakeoff is structurally `PASS` but `SAMPLE_PENDING`; it emitted `served_current`, `conservative_no_market_baseline`, and `clob_overlay_risk_only`, skipped `current_high_trust_retrain` and `dynamic_source_freshness` because probability columns were missing, and all emitted variants had 0 quote-permission rows. The current model stance is harvest-only veto/research overlay, not quote-center or live model-skew driver.
- June 27 midnight rollover readiness is documented at `docs/research/MM_ROLLOVER_READINESS_2026-06-27.md`. The pre-refresh keyless shadow run `data/mm_runs/2026-06-27/20260627T040113401310Z` failed closed with preflight `BLOCK`, 12 `NO_QUOTE_MISSING_PREFLIGHT` rows, 0 quote permissions, and 0 live permissions. At that time, primary blockers were no active current market rows, missing current snapshot/model and source-status rows, missing band-level CLOB features, June 27 exchange-economics target-date mismatch, and missing CLOB token/book/reward metadata for 8 markets. Its bounded paper diagnostic is `data/backtest/mm_paper_shadow_20260627T040113_midnight_probe.json` and remains diagnostic-only historical evidence.
- After rechecking current official Polymarket US docs, June 27 exchange economics were refreshed and accepted: `data/backtest/exchange_economics_publish_20260627_midnight.json` and `data/backtest/exchange_economics_drift_20260627_midnight.json` both pass, material change count is 0, snapshot is `xecon-036874d19e56c76f`, and source hash is `f4dad4615bc83281b5c144bc788ff77c`. The keyless June 27 shadow run `data/mm_runs/2026-06-27/20260627T041123785887Z` improved to preflight `WARN` with 52 no-quote rows, 0 quote permissions, and 0 live permissions. It was still blocked by 44 known-edge permission rows and 8 missing-preflight rows. Its bounded score is `data/backtest/mm_paper_shadow_20260627T041123_after_economics_remediation_fix.json`, and target-date-aligned readiness is `data/backtest/mm_live_readiness_20260627T041123_shadow_target_aligned.json` with status `BLOCK`, 13 blockers, and `live_capital_permission = false`.
- After an explicit safe local-date `market_microstructure capture --market all`, the 04:24Z keyless June 27 shadow run `data/mm_runs/2026-06-27/20260627T042401695036Z` remained preflight `WARN` with 52 no-quote rows, 0 quote permissions, and 0 live permissions. The first failing gate shifted to `model_freshness`; reason counts were 22 `NO_QUOTE_KNOWN_EDGE_PERMISSION`, 22 `NO_QUOTE_STALE_INPUT`, and 8 `NO_QUOTE_MISSING_PREFLIGHT`. Toronto and Miami passed preflight, NYC and Atlanta were stale on model freshness, and eight markets still had June 27 active-event/CLOB/reward/snapshot/source blockers because discovery/capture remained on June 26 event folders. Its bounded score is `data/backtest/mm_paper_shadow_20260627T042401_after_capture_recheck.json`, and target-date-aligned readiness is `data/backtest/mm_live_readiness_20260627T042401_after_capture_recheck.json` with status `BLOCK`, 13 blockers, and `live_capital_permission = false`.
- After refreshing the default event-metadata artifact back to June 27, forcing target-date snapshots, and refreshing public CLOB books/features, all-market June 27 shadow run `data/mm_runs/2026-06-27/20260627T055820610723Z` remained preflight `WARN` with 132 no-quote rows, 0 quote permissions, and 0 live permissions. Snapshot/model/source rows and event metadata are no longer the leading one-shot blocker. Remaining split is 55 `NO_QUOTE_KNOWN_EDGE_PERMISSION`, 44 `NO_QUOTE_STALE_INPUT`, and 33 `NO_QUOTE_INFORMATION_EVENT`; preflight remediation root cause is four historical `clob_book_tape_gap_over_threshold` rows. Its bounded score is `data/backtest/mm_paper_shadow_20260627T055820_after_metadata_snapshot_refresh.json`, and target-date-aligned readiness is `data/backtest/mm_live_readiness_20260627T055820_after_metadata_snapshot_refresh.json` with status `BLOCK`, 11 blockers, and `live_capital_permission = false`. Treat it as all-market no-go evidence.
- A selected-market Austin/Dallas/Houston June 27 shadow subset after the event window cleared, `data/mm_runs/2026-06-27/20260627T061148175884Z`, passed preflight and emitted 16 harvest-only quote-permission rows, 17 no-quote rows, and 0 live-trade-permission rows. Reason split: 16 `QUOTE_HARVEST_MID`, 10 `NO_QUOTE_DISAGREEMENT_SHADOW`, and 7 `NO_QUOTE_MISSING_BOOK`; event gate action was `none` for all 33 rows. Its bounded score `data/backtest/mm_paper_shadow_20260627T061148_subset_austin_dallas_houston.json` has 32 quoted legs, 0 conservative fills, 0 queue-estimated fill legs, fill evidence `BLOCK` due to missing trade-size rows and 32 unresolved resting quotes, counterfactual reward 144.890411 USDC, and actual payout evidence false. Matching readiness `data/backtest/mm_live_readiness_20260627T061148_subset_austin_dallas_houston.json` remains `BLOCK` with 11 blockers and `live_capital_permission = false`. Treat it as useful historical subset evidence, not live authorization.
- Fresh selected-market Austin/Dallas/Houston shadow after targeted snapshot refresh plus targeted public CLOB capture, `data/mm_runs/2026-06-27/20260627T071559212462Z`, is the current point-in-time subset evidence. It passed preflight and emitted 33 no-quote rows, 0 quote-permission rows, and 0 live-trade-permission rows: 25 `NO_QUOTE_SNAPSHOT_CADENCE_DEGRADED` and 8 `NO_QUOTE_MISSING_BOOK`. Its bounded score `data/backtest/mm_paper_shadow_20260627T071559_subset_austin_dallas_houston.json` has 0 quoted legs, 0 conservative fills, 0 queue-estimated fills, reward score 0, net P&L 0, exchange economics `PASS`, and actual payout evidence false. That artifact predates the zero-quote fill-evidence fix, so any fill `PASS` wording is vacuous; newly regenerated zero-quote scores block with `no_quote_legs`. Matching readiness `data/backtest/mm_live_readiness_20260627T071559_subset_austin_dallas_houston.json` remains `BLOCK` with 8 blockers and `live_capital_permission = false`.
- Subset input proofs now isolate Austin/Dallas/Houston from all-market noise: `data/backtest/event_metadata_validation_20260627_subset_austin_dallas_houston.json` is `PASS`; strict CLOB audits for each of Austin, Dallas, and Houston return `ok: true`; the latest subset run summary has preflight `PASS`, fresh model and CLOB ages, and 0 stale-model/stale-CLOB liveness blockers, but quote-time policy still blocks on degraded snapshot cadence from earlier gaps plus missing spread for thin tail books.
- Daily-roll status at `2026-06-27T06:24:17Z` still points at the June 26 post-settlement process and reports `daily_roll_supervisor.start_time_gate.allowed = false`, `start_after_local_time = 19:30`, and expected target `2026-06-27`. The default scheduled roll is therefore not enough for full-window paper evidence. `market_making_daily_roll stop` now exists so the operator can stop the old paper loop before starting the current target date during the active window.
- `src/weather/market/market_making_readiness.py` now has fail-closed `readiness_inputs_target_date_aligned`, `daily_roll_target_date_current`, and runtime-liveness reporting for `blocked_restart_required`, stale `artifact_liveness`, and `operator_report.restart_recommended`, plus target-date-aware default paper-score selection. It also distinguishes one-shot run-summary status from daily-roll status so a one-shot `root_cause_class` such as `policy_no_edge` is not misreported as stale daily-roll runtime identity. Current daily-status readiness `data/backtest/mm_live_readiness_20260627T0642_daily_status_stale_heartbeat.json` selects `data/backtest/mm_paper_postsettlement_recovered_20260627T0233_competitor_source.json`, aligns on target date `2026-06-26`, and remains `BLOCK` with 17 blockers because the supervisor expected target is `2026-06-27`, preflight is blocked, the roll is post-settlement, runtime liveness is stale, fill evidence is blocked, and platform/operator gates are missing. Explicit mismatch readiness `data/backtest/mm_live_readiness_20260627T0625_daily_status_target_mismatch.json` still blocks on target-date alignment when a June 27 paper score is paired with the June 26 daily status.
- Preflight remediation text is now grounded in exposed repo commands: `src/weather/market/market_making_preflight.py` no longer suggests the non-existent `market_microstructure refresh-tokens`; `active_event`, `clob_tokens`, and related CLOB repairs point to date-aware commands such as `python -m weather.market.market_microstructure capture --market all --date <YYYY-MM-DD>`. Snapshot/model/source repairs point to `python -m weather.collection.snapshot_tracker --force --market all --date <YYYY-MM-DD>`. Counted CLOB tape gaps are labeled `clob_book_tape_gap_over_threshold` and no longer imply raw-refresh can make the day countable. `src/weather/market/market_making_readiness.py` can also build readiness from a one-shot run summary by using its `run_folder`.
- Earlier pre-refresh June 27 readiness probe: `data/backtest/event_metadata_validation_20260627_probe.json` passed, while the standard event-metadata artifact was restored to target date `2026-06-26`. The keyless June 27 shadow run `data/mm_runs/2026-06-27/20260627T003709708211Z` failed closed with preflight `BLOCK`, first failing gate `active_event`, first detail `no active current market rows`, 12 `NO_QUOTE_MISSING_PREFLIGHT` rows, 0 quote-permission rows, and 0 live-trade-permission rows. Its `run_summary.json` includes aggregate `preflight_diagnostics`; all 12 markets were blocked by empty `clob_tokens.csv`, event-metadata target-date mismatch in the default artifact, exchange-economics target-date mismatch, missing current snapshot/model rows, missing source-status rows, missing CLOB books/features, missing token/condition ids, missing reward metadata, and no active current market rows. Its paper diagnostic `data/backtest/mm_paper_shadow_20260627T003709708211Z_nextdate_probe.json` scored 12 quote rows, 0 quote legs, 0 quote-permission rows, paper freshness `NO_ACTIVE_DAY`, fill evidence `SKIPPED`, and exchange-economics gate `BLOCK` before the June 27 economics refresh.
- Latest 2026-06-26 future-date shadow drill emitted 12 no-quote rows, 0 quote permissions, and 0 live-trade permissions. Event metadata/economics passed during the drill, but preflight blocked because June 26 snapshot/model rows, source-status rows, CLOB token rows, CLOB books/features, and reward metadata were missing while active loops were still on June 25.
- Current blockers: source-status degradation preflight `BLOCK` across all 12 markets, no countable active-window paper-live-forward quote/fill evidence, live-forward gate `BLOCK`, noncountable one-shot shadow evidence only, latest current quote permission 0, fill evidence `BLOCK`, 46 unresolved resting quote legs on the latest nonzero-quote shadow run, 400 missing-size trade rows on that historical diagnostic, actual liquidity reward payout evidence still 0, model/policy promotion `BLOCK`, absent live-account/platform evidence, absent real private WebSocket reconciliation proof, absent real cancel-all zero-open-order confirmation, and absent live US latency-stopgap handling evidence.

Research questions to answer:
1. Which reward formula applies to our likely operating surface today: Polymarket International or Polymarket US?
2. For the current target date, which weather/climate events have active rewards, min size, target size, tick size, spread threshold, and fee/rebate eligibility?
3. What quote shape maximizes reward score without increasing adverse selection beyond observed paper markouts?
4. Which bands should be harvest-only, no-quote, or edge-research according to `mm_known_edge_map.json`, promotion state, CLOB recon, and source freshness?
5. What minimum paper evidence is required before any one-market, one-band, min-size live pilot?
6. Where does the current model help the bot, and where should it only veto quoting?
7. What operational failures would create real loss: stale books, stale WU/source rows, stale event metadata, wrong platform rules, runtime drift, user stream failure, heartbeat failure, cancel-all failure, or fill reconciliation mismatch?
8. What simulations are missing before pilot: queue fill realism, target-size reward share, adverse-selection markouts around observation windows, platform fee/rebate reconciliation, and full-day budget pressure?

Decision gates:
- PASS only when the artifact or command proves the gate with current target-date evidence.
- WARN when evidence is useful but stale, partial, post-settlement, queue-estimated, or diagnostic-only.
- BLOCK when evidence is missing, stale, internally inconsistent, not countable live-forward evidence, or requires credentials/live exchange mutation.
- Any one of these remains a live NO-GO: missing active-date preflight, missing strict CLOB audit, stale economics, stale event metadata, no quote-permission rows, only post-settlement quote evidence, fill evidence completeness BLOCK, reward estimate not measured, promotion BLOCK, missing user-stream/cancel-all verification, or absent platform/account eligibility proof.

Deliverables:
1. Update or create a dated readiness report under `docs/research`.
2. Update or create a dated repo audit under `docs/research`.
3. Update or create a dated simulation report under `docs/research`.
4. Update or create a small-scale test plan under `docs/research`.
5. If code changes are needed, keep them focused on safety, observability, or simulation correctness. Add or update tests for any code change.
6. End with a concise list of current PASS/WARN/BLOCK gates and the next three safest actions.
```

- Latest readiness reports also expose paper-side quote-starvation diagnostics. For `data/backtest/mm_live_readiness_20260627T073107_current_all.json`, verify `paper_quote_blocked_rows = 109`, `paper_quote_blocked_fraction = 0.825758`, `paper_quote_blocker_reason_counts = {NO_QUOTE_KNOWN_EDGE_PERMISSION: 88, NO_QUOTE_DISAGREEMENT_SHADOW: 11, NO_QUOTE_MISSING_BOOK: 10}`, `paper_quote_blocker_event_gate_suppressed_rows = 11`, and `paper_quote_blocker_stale_input_rows = 0`. These are diagnostics only; they do not relax any readiness gate.
- The same readiness Summary now surfaces the fill/reward/evidence blockers operators must check before promotion: `paper_score_freshness_status = NO_ACTIVE_DAY`, `live_forward_day_count = 0`, `locked_policy_params = false`, `fill_evidence_blockers = [missing_size_trade_rows, unresolved_resting_quotes]`, `missing_size_trade_rows = 400`, `unresolved_resting_quote_count = 46`, `total_reward_score = 20.559525`, `counterfactual_reward_usdc = 176.767745`, `score_at_or_above_target_size = false`, and `actual_payout_evidence = false`.
- Candidate-market diagnostics are now first-class in the paper and readiness outputs: `paper_quote_permission_market_counts = {austin: 4, dallas: 7, denver: 5, houston: 7}` and `paper_top_quote_permission_cells` lists the top 12 `harvest_only` shadow quote cells. Use these only for selecting future paper-live-forward probes; they are not live-trading permission.

## Source Links To Re-check Each Pass

- Polymarket International liquidity rewards: <https://docs.polymarket.com/market-makers/liquidity-rewards>
- Polymarket International fees: <https://docs.polymarket.com/trading/fees>
- Polymarket International maker rebates: <https://docs.polymarket.com/market-makers/maker-rebates>
- Polymarket International order creation: <https://docs.polymarket.com/api-reference/trade/post-a-new-order>
- Polymarket International cancel-all: <https://docs.polymarket.com/api-reference/trade/cancel-all-orders>
- Polymarket International user WebSocket: <https://docs.polymarket.com/api-reference/wss/user>
- Polymarket US liquidity incentives: <https://docs.polymarket.us/incentives/liquidity>
- Polymarket US fee schedule: <https://docs.polymarket.us/fees>
- Polymarket US order API overview: <https://docs.polymarket.us/api-reference/orders/overview>
- Polymarket US authentication: <https://docs.polymarket.us/api-reference/authentication>
- Polymarket US market integrity: <https://integrity.polymarket.us/>
- Avellaneda and Stoikov, inventory-risk market making: <https://people.orie.cornell.edu/sfs33/LimitOrderBook.pdf>
- Gueant, Lehalle, and Fernandez-Tapia, inventory-constrained quoting: <https://arxiv.org/abs/1105.3115>
- Moallemi et al., queue position value: <https://moallemi.com/ciamac/papers/queue-value-2016.pdf>
- Hummingbot liquidity mining whitepaper: <https://hummingbot.org/liquidity-mining.pdf>

# Market-Making Goal Completion Audit

Date: 2026-06-26 local / 2026-06-27 UTC

Scope: requirement-by-requirement audit for the active goal of preparing this repo for a small-scale Polymarket weather market-making test focused on liquidity rewards. This is a preparation audit only. It does not authorize live orders, live-pilot mode, credentials, wallet funding, or irreversible trading actions.

## Verdict

The goal is not complete. The repo is materially better prepared for paper/shadow research, but current evidence proves `NO LIVE CAPITAL`.

Current latest all-market machine-readable gate:

- `data/backtest/mm_live_readiness_20260627T131743_paperlive_active_source_status_block.json`
- schema `mm_live_readiness_v0.2`
- status `BLOCK`
- `blocker_count = 11`
- `live_capital_permission = false`
- `requires_explicit_operator_approval = true`

Current latest selected-subset machine-readable gate:

- `data/backtest/mm_live_readiness_20260627T071559_subset_austin_dallas_houston.json`
- schema `mm_live_readiness_v0.2`
- status `BLOCK`
- `blocker_count = 8`
- `live_capital_permission = false`
- `requires_explicit_operator_approval = true`

Current top blockers:

- Latest all-market and selected-subset evidence are one-shot `shadow` operator drills, so they are not countable active-day `paper-live-forward` evidence.
- Live-forward gate is `BLOCK`.
- Countable paper quote permission is not proven. The freshest all-market one-shot shadow run now fails closed at preflight on `source_status_degradation` across all 12 markets, with 0 quote permissions and 0 live permissions. A forced snapshot/model refresh cleared the prior six-market `model_freshness` blocker, so the durable current blocker is external source-status/provider-auth evidence. The latest nonzero-quote all-market one-shot shadow run has 23 noncountable quote permissions, but it is `operator_drill` evidence with live-forward gate `BLOCK`, fill evidence `BLOCK`, 400 missing-size trade rows, and 46 unresolved resting quotes.
- Model-use readiness is not promotion grade: latest bakeoff is `SAMPLE_PENDING`, two intended variants were skipped for missing probability columns, and all emitted variants had 0 quote-permission rows.
- The latest zero-quote June 27 diagnostic now blocks fill evidence explicitly with `no_quote_legs`. Older zero-quote diagnostics that said fill evidence `PASS` are pre-fix artifacts and do not prove promotion-grade fill/P&L evidence.
- Policy is not locked across enough paper days.
- There are not 14 consecutive countable live-forward paper days.
- Conservative P&L after fees/incentives is 0, not positive.
- Actual reward/rebate payout evidence is absent.
- Operator live-readiness and platform verification artifacts are missing or fail closed.
- June 27 all-market rollover evidence is still not ready, but the current blocker is no longer the explicit CLOB audit. After the latest public CLOB loop recheck, `market_microstructure audit --strict --date 2026-06-27` returns `ok: true` with startup gaps ignored from the current loop start. The freshest all-market shadow run remains noncountable and now preflight-blocked by source-status degradation; it has 0 quote permissions, fill evidence `BLOCK` via `no_quote_legs`, no actual payout evidence, and no active-window `paper-live-forward` gate.

## Requirement Audit

| Requirement | Status | Current Evidence | Remaining Work |
|---|---:|---|---|
| Current Polymarket research | Partial | `MM_REWARD_FARMING_READINESS_2026-06-26.md` cites official US/global docs, fee/reward formulas, `participateDontInitiate`, private WebSocket, cancel-all, rate limits, integrity rules, and US `POST /v1/orders/batched/cancel` cancel batch cap 20 with private-stream final-state reconciliation. The 2026-06-27 same-day official-docs recheck found no fee/reward/tick/min-size drift from the accepted June 27 economics snapshot. | Recheck official docs before any future live date; republish exchange economics only when source values, target date, platform, or eligibility evidence changes. |
| Compare docs to exchange economics | Current for June 27 | Accepted snapshot matches current official Polymarket US fee/reward assumptions; latest drift artifact `data/backtest/exchange_economics_drift_20260627_recheck.json` is `PASS`, `material_change_count = 0`, `rescore_required = false`, and snapshot `xecon-036874d19e56c76f`. `build_snapshot_payload()` defaults now match the accepted US assumptions so fixture-generated simulations do not silently use legacy economics. | Recheck official docs and republish only when source values, target date, platform, or eligibility evidence changes. |
| Market-making technique research | Partial | Docs classify harvest-only, reward-aware, event-window pulling, model veto, model-skewed quoting, queue diagnostics, and manipulative strategies. | Keep model-skewed quoting blocked until per-slice edge, markouts, independent days, and known-edge permission pass. |
| Model-use readiness | Blocked for promotion | `MM_MODEL_USE_READINESS_2026-06-26.md` records the current gate: accepted known-edge map has 17 records; latest model-variant bakeoff is structurally `PASS` but `SAMPLE_PENDING`; skipped variants had missing probability columns; all emitted variants had 0 quote-permission rows. | Use the model only as harvest-mode veto/risk overlay and research signal until countable active-day markouts, complete fills, locked policy, and known-edge permission support model-skewed quoting. |
| Architecture map | Done for current repo shape | `MM_REPO_AUDIT_2026-06-26.md` maps orchestration, policy, preflight, CLOB, paper, risk, lifecycle, budget, exchange adapter, dashboard, daily roll, and tests. | Refresh after significant code movement. |
| Shadow and paper modes never emit live permission | Passing in current evidence | Readiness summary: `paper_score_live_trade_permission_rows = 0`, `latest_tick_live_trade_permission_rows = 0`; focused tests pass. | Continue to gate this in every readiness run. |
| Live-pilot requires explicit flags and live gates | Partially proven | Preflight/platform gate tests cover missing and stale platform verification; readiness always emits `live_capital_permission = false`. | Real platform verification v0.2 and operator approval remain absent; do not run live-pilot. |
| Stale snapshot/model fails closed | Passing in current behavior | Latest moving loop can flip to `WARN` on model freshness and readiness blocks when stale. | Restore active-window model freshness before collecting countable paper evidence. |
| Stale CLOB fails closed | Passing in current behavior | Earlier June 27 target-date diagnostics failed closed on CLOB tape gaps and stale-book rows. The latest explicit `market_microstructure audit --strict --date 2026-06-27` now returns `ok: true`, while readiness remains blocked by noncountable shadow evidence, fill evidence, payout, policy-lock, and platform gates. | Keep strict audit passing for the selected pilot markets on the target date before countable evidence, and do not let a passing CLOB audit override other blockers. |
| Observation trigger fails closed | Passing in current behavior | Readiness includes `observation_trigger_fresh`; 125739 reproduced the fail-closed watcher-stale path with 12 stale watcher incidents. The safe `weather.operations.observation_trigger ensure` path later removed a dead writer lock for PID 1536, started a fresh watcher, and the latest 131743 active-window readiness has `observation_trigger_runtime_root_cause_counts = {}` while still blocking on source-status. | Recheck before any active-day paper promotion. |
| Event metadata target-date mismatch fails closed | Passing in current behavior | June 27 probe failed closed when default event metadata did not match target date; current readiness includes target-date gate. | Refresh event metadata for the actual pilot target date. |
| Exchange economics drift/missing proof fails closed | Passing in current behavior | June 27 probe blocked on missing target-date economics; current June 26 paper score has exchange economics `PASS`. | Publish/accept economics for the actual target date and platform. |
| Source-status degradation fails closed | Passing in current behavior | Latest all-market active-window paper-live-forward `data/mm_runs/2026-06-27/20260627T131743658156Z` blocks preflight with `source_status_degradation` failing across all 12 markets after replay-based source-status backfill rewrote 3,756 rows across the 12 current June 27 folders before the prior 103532 probe and after `snapshot_tracker --force --market all --date 2026-06-27` cleared stale model rows. `preflight_remediation.json` reports `source_status_degradation_blocked = 12`; `run_summary.json` exposes `quote_intent_rows = 132`, `quote_rows = 132`, `no_quote_rows = 132`, 0 quote permissions, and 0 live permissions. | Repair external weather-provider credentials/source health, rerun `python -m weather.collection.snapshot_tracker --backfill-source-status --overwrite-source-status`, then rerun paper-live-forward/readiness before interpreting quote permission. |
| Quote lifecycle releases stale reservations | Partially proven | Cancel-all flag tests and lifecycle/budget tests pass in focused suite. | Need active-day settlement/resting-quote resolution evidence with no unresolved quotes. |
| Budget ledger reconciles open lifecycle risk | Partially proven | Focused lifecycle/budget tests pass; paper score exposes unresolved quotes. | Promotion needs active-day evidence with zero unresolved resting quotes. |
| Event-level worst-case/correlated exposure caps | Partially proven | `mm_risk` focused tests pass. | Verify in active-window paper with selected markets/bands and final initial budget. |
| Paper scoring does not over-credit rewards | Improved but not complete | Reward score and counterfactual reward are separated; actual payout evidence is false. | Reconcile actual reward/rebate payouts before scaling; keep counterfactual rewards out of realized P&L. |
| Fill simulator remains conservative | Partially proven | Conservative fills are separate from queue-estimated fills; latest zero-quote June 27 diagnostic now blocks with `no_quote_legs`, and earlier quoted diagnostics still have fill evidence `BLOCK`. | Collect complete trade sizes, queue-book rows, markouts, settlement, and resting-quote resolution on countable quoted evidence. |
| Queue simulator is companion evidence only | Passing in reports | Docs and paper reports distinguish queue-estimated fills from go-live evidence. | Keep queue evidence out of live gate until conservative evidence is promotion grade. |
| No live credentials/secrets in research/paper commands | Improved, still verify every pass | Work used tests, docs, status, shadow/paper/readiness commands only; no live-pilot or credentialed exchange command was run. Weather.com provider-key defaults were removed from source; the key is now environment-only via `WEATHER_COM_API_KEY` or `WEATHER_COM_KEY`. A status-output leak risk was also fixed: collection source-status rows and health summaries now redact secret-like URL query values such as `apiKey`, including empty `apiKey=` values from provider error URLs. Captured verification of `snapshot_tracker --status` found 0 raw secret-query parameters and 48 redacted secret-query parameters in stdout. Platform verification v0.2 now also rejects exact secret fields and unredacted secret-like URL query values, and requires structured `secret_redaction` proof fields before live-pilot can pass. | Keep live and provider credentials external, configure provider keys outside the repo for data collection, keep status/report redaction tested, and fill the platform-verification v0.2 secret-redaction proof only after fresh scans pass. |
| Run shadow/paper simulations | Partial | Multiple keyless shadow and paper-live-forward diagnostics exist. The latest active-window paper-live-forward one-shot has 0 quote permissions and 0 live permissions; the earlier recovered post-settlement score with 17 quote permissions remains noncountable. | Need fresh active-window paper-live-forward days that count toward live-forward gates. |
| Concrete go/no-go criteria | Done, still no-go | `MM_SMALL_SCALE_TEST_PLAN_2026-06-26.md` and readiness JSON define gates. | Convert to go only after all hard gates pass and explicit operator approval exists. |

## Current Operational State

Daily roll:

- Status `idle_process`, PID `29180`, `pid_alive = true`, terminal status true.
- Action `blocked_restart_required`.
- Mode `paper-live-forward`.
- Evidence mode `post_settlement_evaluation`.
- Latest folder `data/mm_runs/2026-06-26/20260627T031938117215Z`.
- Latest tick: 132 quote rows, 0 quote-permission rows.
- Supervisor `SCHEDULED_WAIT` / `scheduled_wait`; expected target date `2026-06-27`, status target date `2026-06-26`.
- Artifact liveness `STALE_HEARTBEAT_METADATA`; latest useful all-market write is stale and does not count toward live-forward evidence.
- This process is not current June 27 active-window paper-live-forward evidence and does not count for live readiness.

Market data:

- CLOB loop recently reported fresh heartbeat/books, 0 consecutive errors, and no error markets.
- Latest explicit target-date strict CLOB audit at `2026-06-27T07:52:34Z` returned `ok: true` for all 12 markets, with startup gaps ignored before the current loop cutoff. This supersedes the earlier same-day four-gap CLOB failure, but it does not make earlier one-shot shadow evidence countable paper-live-forward evidence.
- Managed CLOB loop controls now accept fixed `--date YYYY-MM-DD` for `loop`, `start-detached`, `restart`, and `ensure`, and loop health now reports date-selection metadata plus per-market event dates. This closes the cross-timezone-midnight ambiguity where `status` could look fresh while west-coast books were written to prior-local-date folders. The currently running loop predates this patch and still reports `date_selection = market_local_date`; restart it with a fixed date before collecting countable paper evidence.
- Snapshot tracker recently reported `RUNNING` with runtime identity current. The saved fleet summary has 12 `COLLECTING` markets and snapshot cadence `PASS`, but it does not have clean promotion evidence: early-hour coverage is `BLOCK`, and `source_status_proof.status = BLOCK` with `root_cause_class = settlement_source_auth_failure` blocks 12/12 markets across trading, live-permission, and promotion lanes.
- Promotion-quality snapshot, source-status, and early-hour evidence therefore remain blocked even though the current loop is alive.

Operational incidents preserved as evidence:

- Historical `OSError: [Errno 28] No space left on device` occurred during prior write attempts; latest disk check showed about 223 GB free, so this is not the current blocker but should stay in the runbook as a capacity risk.
- Historical `MemoryError` occurred while reading promotion state; current readiness still blocks promotion, so this has not created live risk, but large promotion artifacts should be bounded or streamed before scale.
- Snapshot/source status secret hygiene was tightened after `snapshot_tracker --status` showed provider error URLs could include query credentials. `src/weather/collection/redaction.py`, `snapshot_store.py`, and `collection_health.py` now redact secret-like URL query values before persisting new source-status rows or rendering health summaries, including summaries built from older unredacted rows; empty sensitive query values such as `apiKey=` are treated as unredacted until rewritten as `apiKey=<redacted>`. Current verifier output for `snapshot_tracker --status` shows 0 raw secret-query parameters and 48 redacted secret-query parameters. `model_constants.py` and `wu_history.py` no longer embed Weather.com provider-key defaults; this shell currently has neither `WEATHER_COM_API_KEY` nor `WEATHER_COM_KEY` set, so source collection should fail closed until the operator configures credentials externally. Platform verification now imports the same redaction detector, rejects unredacted secret-like query material anywhere in the v0.2 artifact, and requires `secret_redaction.status_output_verified`, `source_doc_scan_verified`, `generated_artifact_scan_verified`, `no_unredacted_secret_findings`, and `scan_scope` before a live-pilot gate can pass.
- June 27 exchange economics have now been refreshed and accepted after rechecking official Polymarket US docs. Latest artifact `data/backtest/exchange_economics_drift_20260627_latest.json` is `PASS`, material change count is 0, and rescore required is false.
- Latest nonzero-quote all-market June 27 one-shot shadow evidence is `data/mm_runs/2026-06-27/20260627T073107208602Z`: preflight `PASS`, 23 quote permissions, 109 no-quote rows, 0 live permissions, exchange economics `PASS`, live-forward gate `BLOCK`, and root cause `trading_permissions_emitted`. The split is 23 `QUOTE_HARVEST_MID`, 88 `NO_QUOTE_KNOWN_EDGE_PERMISSION`, 11 `NO_QUOTE_DISAGREEMENT_SHADOW`, and 10 `NO_QUOTE_MISSING_BOOK`. The matching paper diagnostic is `data/backtest/mm_paper_shadow_20260627T073107_current_all.json`.
- Exact quote-permission market counts for that diagnostic are now machine-readable: Austin 4, Dallas 7, Denver 5, and Houston 7. The top quoted cells are all `harvest_only` / `SHADOW` / `QUOTE_HARVEST_MID`, so they are paper-probe candidates, not promotion-approved live cells.
- Target-date-aligned all-market readiness is `data/backtest/mm_live_readiness_20260627T073107_current_all.json`: `BLOCK`, blocker count 11, `live_capital_permission = false`; event metadata, exchange economics, preflight, CLOB freshness, snapshot/model/source freshness, observation trigger, and paper-mode live-permission safety pass, but active-day live-forward evidence, live-forward gate, countable quote permission, fill evidence, policy lock, 14-day evidence, conservative P&L, unresolved resting quotes, actual reward payout, operator readiness, and platform verification still block. One-shot run-summary latest tick counts now report 132 quote rows and 23 quote-permission rows.
- Preflight remediation command text was corrected so missing active-event and CLOB-token incidents point to `python -m weather.market.market_microstructure capture --market all`, an exposed command, rather than the stale `refresh-tokens` string.
- Continuation recheck at `2026-06-27T12:20:30Z`: `weather.market.exchange_economics drift --target-date 2026-06-27 --platform polymarket_us --json-out data\backtest\exchange_economics_drift_20260627_recheck.json` wrote `data/backtest/exchange_economics_drift_20260627_recheck.json` with `status = PASS`, `material_change_count = 0`, `rescore_required = false`, and snapshot `xecon-036874d19e56c76f`. Focused exchange-economics, paper, and daily-refresh regression tests passed (`97 passed in 69.84s`). Earlier same-morning CLOB status was `RUNNING` with fresh books and `date_selection = market_local_date`; explicit target-date strict audit was `ok: true`. Daily roll still pointed at June 26 post-settlement evidence with `action = blocked_restart_required`, supervisor `SCHEDULED_WAIT`, expected target date `2026-06-27`, start gate before `19:30` local, `artifact_liveness = STALE_HEARTBEAT_METADATA`, and `current_counts_toward_live_forward_gate = false`.
- Follow-up one-shot shadow at `data/mm_runs/2026-06-27/20260627T075907746405Z` passed preflight after the CLOB/economics recheck but emitted 132 no-quote rows, 0 quote-permission rows, and 0 live-trade-permission rows. Reason split: 88 `NO_QUOTE_KNOWN_EDGE_PERMISSION`, 35 `NO_QUOTE_SNAPSHOT_CADENCE_DEGRADED`, and 9 `NO_QUOTE_MISSING_BOOK`. Its pre-fix score `data/backtest/mm_paper_shadow_20260627T075907_current_all.json` has 0 quoted legs, reward score 0, and readiness `data/backtest/mm_live_readiness_20260627T075907_current_all.json` is `BLOCK` with 8 blockers; any zero-quote fill-evidence `PASS` wording in that artifact is vacuous and not promotion proof.
- Earlier one-shot shadow after the known-edge provenance patch, `data/mm_runs/2026-06-27/20260627T080312460577Z`, had preflight `STALE`, first failing gate `model_freshness`, 132 `NO_QUOTE_STALE_INPUT` rows, 0 quote-permission rows, and 0 live-trade-permission rows. Its run summary records the loaded known-edge map: `data/backtest/mm_known_edge_map.json`, schema `mm_known_edge_map_v0.2`, `record_count = 17`, `diagnostic_only = false`, permission counts `{harvest_only: 7, edge_research: 3, no_quote: 7}`. The regenerated score marks 0 quote legs as non-promotion fill evidence (`fill_evidence_blockers = [no_quote_legs]`, `fill_evidence_vacuous = true`), and matching readiness `data/backtest/mm_live_readiness_20260627T080312_current_all.json` is `BLOCK` with 11 blockers, including preflight/model freshness, no countable quote/fill evidence, P&L, payout, operator, and platform gates.
- Freshest current active-window paper-live-forward one-shot is `data/mm_runs/2026-06-27/20260627T131743658156Z`: keyless `paper-live-forward`, preflight `BLOCK`, first failing gate `source_status_degradation`, 132 quote-intent rows, 132 no-quote rows, 132 `NO_QUOTE_MISSING_PREFLIGHT` rows, 0 quote-permission rows, and 0 live-trade-permission rows. Before the prior 103532 probe, the documented source-status backfill was applied to all 12 current June 27 folders and rewrote 3,756 rows from replay inputs; a follow-up forced snapshot/model refresh then cleared the stale model rows. Fresh source-status proof still reports `source_status.status = BLOCK`, `root_cause_class = settlement_source_auth_failure`, `source_status_blocked_market_count = 12`, `settlement_auth_failure_source_count = 12`, `weather_com_credential_present = false`, and `weather_com_credential_values_redacted = true`. `preflight_remediation.json` reports `source_status_degradation_blocked = 12`. Matching score `data/backtest/mm_paper_paperlive_20260627T131743_active_source_status_block.json` has paper freshness `PASS`, 0 quoted legs, fill evidence `BLOCK`, reward score 0, exchange economics `PASS`, no payout evidence, paper-day collection gate `OPEN`, and `live_capital_gate_status = NOT_EVALUATED_BY_MM_PAPER`. Matching readiness `data/backtest/mm_live_readiness_20260627T131743_paperlive_active_source_status_block.json` is `BLOCK` with 11 blockers and renders `evidence_mode = active_day_live_forward`, `current_counts_toward_live_forward_gate = false`, `preflight_remediation_root_cause_counts = {source_status_degradation_blocked: 12}`, `observation_trigger_runtime_root_cause_counts = {}`, `snapshot_model_source_failing_gate_counts = {source_status_degradation: 12}`, `source_status_degradation_failed_markets = all 12 selected markets`, `model_freshness_failed_market_count = 0`, and `latest_tick_no_quote_rows = 132`. Same-time local-state checks show CLOB audit and exchange-economics drift pass; remaining local evidence blockers are source-status proof `BLOCK`, daily-roll noncountability, 0 quote permissions, and no promotion-grade fill/P&L/payout evidence.
- Aggregate active-window paper-live-forward evidence confirms the same result across every eligible June 27 active paper-forward folder currently selected by the bounded latest-five query. `data/backtest/mm_paper_paperlive_20260627_latest5_active_source_status_block.json` selected `111731`, `112944`, and `131743`, totaling 396 quote rows, 0 quote permissions, 0 live permissions, 0 quoted legs, fill evidence `BLOCK` with `no_quote_legs`, exchange economics `PASS`, total reward score 0, and no actual payout evidence. Matching readiness `data/backtest/mm_live_readiness_20260627_latest5_paperlive_active_source_status_block.json` remains `BLOCK` with 11 blockers.

## Current Next Actions

The latest nonzero-quote all-market readiness Summary includes non-gating paper blocker diagnostics: `paper_quote_blocked_rows = 109`, `paper_quote_blocked_fraction = 0.825758`, `paper_quote_blocker_reason_counts = {NO_QUOTE_KNOWN_EDGE_PERMISSION: 88, NO_QUOTE_DISAGREEMENT_SHADOW: 11, NO_QUOTE_MISSING_BOOK: 10}`, `paper_quote_blocker_event_gate_suppressed_rows = 11`, and `paper_quote_blocker_stale_input_rows = 0`. Use these fields to separate known-edge/promotion blocks, event suppression, and missing-book failures before changing policy.

It also exposes the promotion-evidence blockers directly: `paper_score_freshness_status = NO_ACTIVE_DAY`, `live_forward_day_count = 0`, `locked_policy_params = false`, `fill_evidence_blockers = [missing_size_trade_rows, unresolved_resting_quotes]`, `missing_size_trade_rows = 400`, `unresolved_resting_quote_count = 46`, `total_reward_score = 20.559525`, `counterfactual_reward_usdc = 176.767745`, `score_at_or_above_target_size = false`, and `actual_payout_evidence = false`.

The latest 131743 readiness artifact records `source_status_weather_com_credential_present = false`, `source_status_weather_com_credential_present_by_var = {WEATHER_COM_API_KEY: false, WEATHER_COM_KEY: false}`, and `source_status_weather_com_credential_values_redacted = true`; upstream `snapshot_tracker --status` exposes the same booleans in `source_status_proof_v0.2` at `source_status_proof.summary.provider_credential_environment` / `weather_com_credential_*`, and fleet observability renders the fields while remaining `CRITICAL`. These fields explain why the top source-status actions start with configuring Weather.com credentials externally rather than only rerunning local backfill. The live-forward SLO recovery checklist is now credential-aware too: with both env vars absent, its first source-status blocker reports `settlement_source_auth_failure_missing_weather_com_credentials`, owner `external Weather.com provider credentials`, and the repair text says to configure `WEATHER_COM_API_KEY` or `WEATHER_COM_KEY` outside the repo before running source-status backfill.

Use `data/backtest/mm_live_readiness_20260627T131743_paperlive_active_source_status_block.json.next_actions` as the latest all-market paper-live-forward machine-readable queue, use `data/backtest/mm_live_readiness_20260627T130826_current_source_status_block.json.next_actions` for the latest shadow/operator-drill comparison, use `data/backtest/mm_live_readiness_20260627T071559_subset_austin_dallas_houston.json.next_actions` for selected-subset historical comparison, and use the daily-roll readiness artifact separately for the moving June 26 post-settlement loop. The current daily-status artifact is `data/backtest/mm_live_readiness_20260627T0642_daily_status_stale_heartbeat.json`; it selects same-target June 26 paper evidence, blocks first on `daily_roll_target_date_current` because the supervisor expected target is June 27, and exposes `blocked_restart_required` / `STALE_HEARTBEAT_METADATA` / `stale_heartbeat_metadata` in the Summary and `daily_roll_runtime_identity_current` gate. The earlier same-target artifact `data/backtest/mm_live_readiness_20260627T0630_daily_status_same_target.json` remains useful for the target-date-current regression, and the explicit mismatch artifact `data/backtest/mm_live_readiness_20260627T0625_daily_status_target_mismatch.json` deliberately blocks on `readiness_inputs_target_date_aligned` if a June 27 paper score is paired with June 26 daily status. At this audit point the latest all-market JSON priority order is:

1. `latest_preflight_passes`: configure `WEATHER_COM_API_KEY` or `WEATHER_COM_KEY` outside the repo, rerun snapshot collection/source-status backfill, then rerun keyless shadow/readiness.
2. `snapshot_model_source_fresh`: configure `WEATHER_COM_API_KEY` or `WEATHER_COM_KEY` outside the repo, rerun snapshot collection/source-status backfill, then rerun keyless shadow/readiness.
3. `active_day_live_forward_evidence`: collect active-window `paper-live-forward` evidence for the current target date. Current daily-roll status is still a prior-target post-settlement process waiting on the default `19:30` local start gate; use the guarded paper-loop stop/start path during the `07:00`-`20:00` local evidence window for full-window paper evidence.
4. `live_forward_gate_passes`: resolve live-forward gate blockers before interpreting paper evidence.
5. `quote_permission_present_in_countable_paper`: first obtain active-day countable paper evidence, then diagnose quote starvation.
6. `fill_evidence_complete`: collect trade sizes, queue-book evidence, markouts, settlement, and resting-quote resolution.
7. `fourteen_consecutive_countable_live_forward_days`: collect consecutive active-day paper-live-forward runs before risking capital.
8. `positive_conservative_pnl_after_costs`: do not trade live until conservative markout/P&L evidence is positive.
9. `actual_reward_payout_evidence`: reconcile predicted rewards/rebates against actual payout artifacts before scaling.
10. `operator_live_readiness_file_passes`: create a live-readiness JSON only after wallet, allowance, heartbeat, user-stream, and cancel-all readiness are proven.
11. `platform_verification_v0_2_passes`: refresh `mm_platform_verification_v0.2` with maker-only, private-stream, cancel-all, latency-stopgap, and secret-redaction proofs.

## Completion Criteria

Do not mark the active goal complete until current evidence proves all of the following:

- Official Polymarket docs have been rechecked for the actual pilot target date and platform.
- Exchange economics snapshot is accepted for that target date.
- Event metadata validates for that target date.
- CLOB strict audit passes for selected markets.
- Snapshot/model/source-status rows are fresh and active-window coverage is countable.
- Observation trigger is fresh.
- Daily-roll runtime identity is current and not circuit-open.
- Live-forward gate passes on active-day `paper-live-forward` evidence.
- Countable paper quote permissions are nonzero.
- Paper/shadow live-trade permission rows remain 0.
- Model-variant bakeoff is no longer sample-pending, intended variants are present, and model-skewed behavior is backed by known-edge permission plus positive countable markouts.
- Fill evidence completeness is `PASS` and promotion grade.
- Policy hash is locked across the required evidence window.
- At least 14 consecutive countable live-forward paper days exist, unless the operator explicitly chooses a smaller paper-only exploratory threshold.
- Conservative P&L after fees, incentives, rebates, markouts, and flattening is positive.
- Unresolved resting quote count is 0.
- Actual reward/rebate payout evidence exists before scaling.
- `data/backtest/live_readiness.json` passes.
- `data/backtest/mm_platform_verification.json` is current v0.2 and passes.
- Live credentials remain outside the repo and no secret values are printed or committed.
- Kill switch/cancel-all is proven with zero open orders after request.
- Any future US batch-cancel path chunks `POST /v1/orders/batched/cancel` requests at 20 orders or fewer and reconciles final state with the private order stream because `canceledOrderIds` is only an echo/submission record.

# Workstation handoff — 2026-07-30f: build the second clock, then the bootstrap

Your keystone report was the best handback of this project. It found a real defect in my own daily
instrument, gave a mechanism fingerprint that proves itself, and corrected two things I had told the
operator. This handoff acts on all of it.

## Status changes since you handed back

**July 24 is repaired, and it recovered a record rather than losing one.** Your pre-flight framing
was right that it needed review; the review overturned my plan twice.

- `replay_input_status.json` records `captured_count: 194`, which reads like a seal — but it was
  generated `2026-07-25T15:06:57Z`, **after** the corruption, by a reader that had already skipped
  the malformed line. 194 was a *consequence* of the defect. Trusting it would have argued for
  discarding record B.
- Accounting all 196 snapshot ids against the 194 intact inputs left exactly two uncovered, and they
  *were* the two fragments: **A = `20260724T053437445479-0400`** (05:34:37, truncated at 8,155
  bytes, remainder never written) and **B = `20260724T054342416541-0400`** (05:43:42, intact,
  154,458 bytes). B was a real record the corruption had merely hidden.

Fragment A quarantined with the full pre-repair file and a receipt; line 46 rewritten as B alone via
atomic replace; `replay_input_status.json` regenerated by its own producer
(`replay_status_backfill.repair_folder(overwrite=True, reconstruct_missing=False)`) so
`reconstructed_count` stays 0 as your predicate requires. Verified: **195 lines / 0 malformed**,
195 covered + 1 legitimately uncovered = 196, status `captured 195 / evaluation_only 1 /
reconstructed 0 / snapshots 196`, and **197,777 JSONL lines across all 13 files of the day parse
clean**. Toronto 07-24 captured count **194 → 195**.

**Your digest and mine agree.** `8f17b3b1...` is over the exact line bytes *including CRLF*; the
stripped body gives `5886dd9c...`. Convention only — both hosts hold identical bytes, which
independently confirms the mirror is faithful. I had flagged that as a possible integrity problem;
it was not.

**Your streak finding is fixed in `streak_status.py`** (master `310eba1b`): tightened to exactly
`complete` to match `latest_toronto_lock_revisions`, demoted grades now print as a named reason, and
I deleted the tool's own false claim to be "the gate to release #1". No ledger row has ever carried
`manual_override`, so the divergence was latent and 8/14 was never inflated.

**Mission 1 of `-30e` is declined, not deferred.** I am not approving removal of the deny-write ACL.
You have 116 GiB free (the operator has since cleared more — ~100 GiB), the MM floor is 66, so the
32 GB is not needed; and the mirror's own purge is a sanctioned writer that needs no ACL change,
which I can drive from this host. Your junction verification and receipt were exactly the right
work — the guard did its job by making the answer checkable.

**Disk work is deferred by operator decision.** A long-term 500 GB cap on your host plus proper use
of the 2 TB cold tier is wanted, but explicitly *later*. Do not spend this cycle on it, and do not
run the `-30e` cold-tier build yet.

## Mission 1: build the second clock

This is the highest-value thing you can build, because it converts the lock from a gamble into a
measured state. You specified it; now make it real. From your own report, a date is
release-admissible when all of the following hold:

- the settlement ledger history verifies and the latest date revision is exactly `complete`;
- the folder resolves to the exact Toronto market/date and contains the canonical snapshot and
  captured-input tapes;
- every bounded JSONL line parses strictly, with no duplicate/conflicting snapshot identity and no
  reconstructed input;
- each pinned snapshot is present in the tape, its captured-input self-hash verifies under
  `captured_input_payload_sha256`, and its recorded distribution is finite, nonnegative, and
  mass-preserving;
- settlement unit/winning-band identity, feature-quality quarantine, tape inventory, and immutable
  file hashes pass the production source checks.

Deliver:

1. A module plus CLI that computes that grade for one date, **streaming** the bounded files, and
   persists a per-date receipt carrying PASS/BLOCK, the **specific** blocking reason, and the input
   hashes. Honour your own warning: this runs once after daily settlement, and the frequent
   host-status monitor must never rescan hundreds of megabytes of hot tape.
2. Contiguous-PASS collapse over dates, exactly as `streak.ps1` collapses ledger grades, so there is
   a single **release-admissible streak** number beside the operational one.
3. Run it over **2026-07-16 .. 2026-07-29** and report the per-date table with reasons. This
   independently verifies my July 24 repair with the real strict reader and the real self-hash — if
   my repair did not actually clear that date, I want to hear it from your tool, not from my own
   scripts.
4. Tell me the cheapest correct way to read that number daily on the production host. If it is a
   JSON receipt I can collapse in `status.ps1` without touching tape, say so and give me the shape.

The point is to know before Aug 3 whether the window will pass, rather than discovering a blocker on
lock day with zero slack. We just avoided exactly that failure by five days, by luck.

## Mission 2: the 12-market all-shadow bootstrap entry point

Your contract verdict is that a literal incumbent-only release is unsupported, but the practical
shape is supported: freeze a `research_only` release, give every runtime market an explicit `shadow`
route with a complete exact base-model serving graph, leave `promote_markets` empty, verify it
inactive. You also identified the exact blocker — the nightly orchestration CLI is **F-only**, so
constructing a twelve-market bootstrap needs a reviewed artifact-build entry point or a bounded
direct use of the existing candidate freezer, and **no serving design change**.

Build that entry point. Produce a **verified inactive** twelve-market all-shadow release (Toronto
plus the eleven F markets), with Toronto bound through a `shadow` route using the seven canonical
Toronto base component artifacts that already exist.

**Do not create a pointer.** `serving_identity_bootstrap` stays gated behind a separate reviewed
boundary decision that is not in this handoff. Instead, report precisely what creating the first
pointer would require, so the operator can approve or decline that as its own decision with the
evidence in front of them.

Why this matters and why it is second rather than first: on Aug 3, a perfect streak plus an
admissible window still leaves us with **nothing to release**, and the lock opens only a 7-day build
window. This is also the step that eventually makes the incumbent-versus-preblend lane switch
actionable — worth `0.016131`, which is 1.74x the entire remaining research gap.

## Mission 3, only if 1 and 2 are done

Resume the MM work that stopped at the admission floor: the 147 pending events, then analysis,
rewards, and cool-bias scoring. You have the headroom now. The 17 validation gaps stay frozen under
the existing contract unless your 1,619-row-set equivalence gate can actually run — do not land the
uniform amendment without it.

## Priority and rationale

1, 2, 3 in order. Mission 1 is small, protective, and tells us whether Mission 2 matters this cycle.
Mission 2 is the critical path to release #1. Mission 3 is real work that is no longer blocked.

## Guardrails

Unchanged, plus two specific to this handoff:

- **Base your work on your own keystone branch, not on master.** `ea0167a7` is not merged.
  `captured_input_payload_sha256` is what Mission 1 depends on, and
  `src/weather/collection/snapshot_store.py` is imported by the capture loops, so that merge is
  roll-sensitive and its timing stays with me — I will land it in a quiet window. Do not assume it
  is in master, and do not merge it yourself.
- The already-pushed forward-shadow branch predates that helper and must adopt it when rebased. Its
  insertion-order diagnostic must never become an authorizing compatibility path.

Otherwise: `data/` read-only under the deny-write ACL, outputs under one declared run root outside
the mirror, topic branches only, no PR, no merge, no master push, no promotion, no pointer change,
no serving change, no scheduler or capture change, no mirror topology change, never read or expose
the sync credential. No ACL modification anywhere — that request is declined for this cycle.

## Handback

`docs/roadmap/agent-report-<date>-workstation-second-clock.md`: the per-date release-admissible
table for 07-16..07-29 first (including the July 24 verdict after my repair), then the daily-read
recipe for the production host, then the bootstrap entry point and what a first pointer would
require. **Push the branch before you start**, and push again at handback.

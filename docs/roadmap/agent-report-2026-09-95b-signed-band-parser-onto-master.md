# 95b — signed native band parser

**IMPLEMENTED; workstation verification recorded below. Production adoption remains separate.**
Serves Q-09. Branch `codex/signed-band-parser-20260924`, based on the explicitly
requested fetched handoff tip `69c5a325342b99fa03655883436e169159b7756d` rather than
the handoff's default master base. The branch therefore includes that handoff
branch's pre-existing commits relative to master; review the mission diff from
the stated base. No stale audit or maker branch was merged.

## Change and evidence

Extracted only the parser and its consumers/tests from
`016e1c92c006c37eb321e9a549eb3e83b1f19fff`. The complete-label parser retains
native signs and zero endpoints, supports `to` and Unicode ranges, rejects
inverted/mixed-unit/contradictory input, and sorts an open lower tail before
negative exact bands. Serving, settlement selection and maker/taker scoring
share its interpretation. No conversion, WU cutoff, trusted floor or model
distribution logic changed. The negative-band partition sums to probability one.

Recorded Toronto labels from the base's tracked
`config/location_market_events.json` are retained in
`tests/fixtures/toronto_signed_band_replay.json`: two target dates, September 23
and 24, 2026. Expected complete serialized bin rows were generated by the base's
`PresentationMixin.market_bins`; the revised output is compared byte-for-byte.
This is label replay, not a reconstruction of historical served forecasts.
Synthetic signed examples cover negative, zero-crossing and open ranges;
no statistical interval is claimed for deterministic tests.

The next free roadmap number was 333. Its item, primary index and generated
backlog are added, and the forward plan's nonexistent item 335 citation is fixed.

## Roll boundary

The repository command returned **UNDECIDABLE: no live closure evidence** (exit 1).
All four required status files are absent from this isolated workstation tree.
No mirror or production status was substituted; actual per-closure membership
cannot be established here. Production must run the command below against its
current retained closures before adopting this branch.

| Changed file | Workstation per-file roll finding |
| --- | --- |
| `src/weather/units.py` | Importable; closure membership unknown, treat as sensitive pending host verdict |
| `src/weather/model/model_presentation.py` | Importable serving code; closure membership unknown |
| `src/weather/backtesting/settlement_io.py` | Importable; closure membership unknown |
| `src/weather/backtesting/settlement_ledger.py` | Importable; closure membership unknown |
| `src/weather/market/mm_paper_scoring.py` | Importable; closure membership unknown |
| `src/weather/market/taker_bot_scoring.py` | Importable; closure membership unknown |
| `src/weather/market/taker_bot_strategy_evaluation.py` | Importable; closure membership unknown |
| All changed `tests/` and `docs/` paths | Roll-free by delegation contract |

No registration, production write, restart, master merge, training, promotion,
credential access or live order was performed.

## Reproduction

From the branch's repository root, resolve the shared interpreter for either a
normal checkout or a linked worktree:

```powershell
$missionRoot = (Resolve-Path .).Path
$missionGit = (& git rev-parse --path-format=absolute --git-common-dir).Trim()
$missionPython = Join-Path (Split-Path $missionGit -Parent) 'venv/Scripts/python.exe'
$missionArgs = @('-m','pytest','tests/model/test_units.py','tests/model/test_market_units.py',
 'tests/model/test_signed_band_replay.py','tests/market/test_native_band_scoring.py',
 'tests/backtesting/test_settlement_ledger.py','tests/backtesting/test_settlement_units.py',
 'tests/backtesting/test_settlement_authority.py','tests/model/test_estimate_distribution.py',
 'tests/market/test_market_making_run.py','tests/market/test_taker_bot.py',
 'tests/reporting/test_roadmap_backlog.py','tests/operations/test_import_architecture.py',
 '-q',('--basetemp=' + (Join-Path $env:TEMP 'weather-95b-tests')))
$missionEncoded = [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes((ConvertTo-Json -Compress -InputObject $missionArgs)))
& ./scripts/ops/workstation_heavy.ps1 -Kind pytest -PythonPath $missionPython -ArgumentsBase64 $missionEncoded -RepoRoot $missionRoot
& ./scripts/ops/roll_verdict.ps1 -Branch origin/codex/signed-band-parser-20260924
```

The capture host must use its admitted bounded verification path and current
load policy, not the workstation wrapper. Remove only the owned test basetemp
after execution.

## Verification and publication

All selected checks are resolved. Initial focused run: 219 passed plus 47
subtests, with four setup failures (the fixture expected three dates instead of
the recorded two; item metadata; new files not staged; backlog generation).
After correcting those: 104 passed in the replay, settlement authority/units,
distribution and architecture expansion. Its remaining roadmap parity failure
ran before generation; generation then succeeded, and the final roadmap plus
canonical-doc audit run passed all 13 checks. `compileall -q src/weather tests`
and `git diff --check` passed. Counts are per run and overlap; they are not an
aggregate independent-test count. The fixture covers 22 recorded labels.

The temporary preparation adapter generated the fixture using the base's
presentation source, and called the canonical roadmap generator. A separate
temporary test called `weather.operations.agent_docs_audit.main([])` and
`weather.reporting.roadmap.roadmap_backlog.main(['--fail-on-lint','--check'])`
under the same admitted pytest wrapper. Neither adapter is a runtime entrypoint.

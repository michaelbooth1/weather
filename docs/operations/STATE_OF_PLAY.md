# State of play

**Last rewritten: 2026-08-25 18:38 America/Toronto.** Read this first; read
`ESTABLISHED_FINDINGS.md` and `RETRACTED_AND_FALSE_LEADS.md` before research.

> **REWRITTEN, never appended. Capped at about 90 lines.** Put quantitative
> evidence, false claims, and durable mechanics in their owning canonical file.

**Objectives:** protect capture/settlement; test whether International maker economics work after all costs; improve forecast risk control. **No market edge is proved.**

## Current truth

| Area | State / next action |
| --- | --- |
| Production | `HEAD == master == origin/master == 4feef39a44f920affcb05387a8882fb5f735cfa0`; no overnight integration changed production. Preserve the two expected fleet-generated location-config modifications. |
| Capture | Snapshot, CLOB, and observation capture remained protected. Do not spend the 12:00-18:00 graded or 18:00-00:30 near-close window on ad-hoc heavy verification. |
| Failed attempt | `credential-reconcile-0825-a2` ran its lawful exact-tip integration preflight at 00:30. Of 143 tests, 134 passed and nine deterministic contract/fixture tests failed. The bounded full suite did not start. The 03:10 merge refused before Git mutation. |
| Recovery evidence | Attempt a2 is immutably closed `FAIL/ABANDONED`; closure SHA-256 is `32b1757fcfc80f4602c9f95cd859b059f3911d1b514e2db346f613d59b7d92bd`. Both exact tasks are Disabled. Its reviewed `manual_reviewed_change` dispatch SHA-256 is `0f63494581612d5cb5401fb6cb8082fbcc8751a10ad4dcfc1938d5bbed68c057`; it grants one reviewed descendant opportunity, not network/topic-publication, Scheduler, credential, exchange, or production authority. |
| Successor | No successor manifest, predecessor claim, registration receipt, or successor tasks exist. Do not describe dispatch-only recovery as armed. The spent a2 helper and schedule are not reusable. |
| Runner bootstrap | Clean local branch `codex/offline-runner-bootstrap-20260825` is frozen at `31151239093756f672a84d7ea846a37cdf27a12c`. It is a minimal roll-free prerequisite for qualifying the full descendant with a production-adopted runner; it is neither published nor merged and carries no Scheduler, credential, exchange, or live authority. |
| Candidate | The isolated `codex/overnight-recovery-hardening-20260824` branch has completed protected-window-safe diff, PowerShell-AST, authority-boundary, CI-startup, and adversarial static review. It remains executable-unqualified: static review, topic publication, or CI is not production-host qualification or production code. |
| Verification clock | The Aug 25 admitted host window has closed. The earliest lawful host qualification is Aug 26 at 00:30. It must run the deterministic preflight and complete bounded suite from the isolated exact-tip worktree before any successor is created or enabled. |
| Earliest integration | Even after a clean Aug 26 qualification, a new immutable successor must use a later complete suite/merge window. Its integration is no earlier than Aug 27. A failure, drift, schedule-feasibility refusal, or review finding moves that date later. |
| Live money | A production merge is necessary but not sufficient. The first live test remains attended and separately gated by fresh compare-only credentials, eligible physical-location/no-circumvention evidence, official geoblock PASS, economics and paper candidate, baseline acceptance, identity/doctor, collateral/zero-state, sealed wrappers, and each literal at the actual mutation boundary. |
| Other debt | The documentation transaction for integration `4feef39a4` remains a separate reviewed closeout before attended live readiness. The partial Stage-A chain and August 17 settlement hole are not evidence that the maker pilot is ready. |

## What the a2 failure means

- The safety controls succeeded: deterministic failures stopped the full suite,
  the merge refused, and production, credentials, and the exchange were untouched.
- The preparation process failed: static/parser/direct checks were treated as
  if they predicted the exact scheduled preflight. They did not.
- The nine failures were repository-local and knowable before registration:
  one stale diagnostic assertion, four bare topic-ref fixtures where
  `origin/...` is required, one synthetic repository without canonical-origin
  proof, and three quiet-merge assertions expecting the retired raw-fetch path.
- CI, pre-arm qualification, overnight drift detection, and Scheduler/runtime
  evidence are separate layers. No one layer may be presented as all four.
- Detailed causes and the hardening boundary are in `docs/roadmap/agent-report-2026-08-25-overnight-prearm-qualification-audit.md`.

## Closed decisions -- do not relitigate without new evidence

- International Polymarket only; never use Polymarket US for a new probe,
  credential path, readiness decision, or mutation.
- The first live test is one bounded plumbing/evidence session: Stage 0 heartbeat/cancel-all
  plus one smallest-valid post-only BUY for each Stage 1
  cancellation mode, at most 10 pUSD each and a non-raisable 100 pUSD wallet cap.
- Candidate selection is never authorization. A successful no-fill test does
  not prove edge, profitability, fill quality, rebates, or Stage 2 readiness.
- No alpha or paid weather provider. Baseline acceptance is informed operator action and is never scheduled automatically.
- No order from a blocked location and no circumvention. Repository timezone is
  scheduling configuration, never evidence that the host is in Ontario.
- No unattended first live session. The supported fixed-session lane is
  `[00:30, 09:00) America/Toronto` with the operator awake and present.

## Immediate execution order

1. Publish and review the exact runner-bootstrap tip, then land that roll-free
   prerequisite through the production-adopted predecessor workflow. Do not
   let not-yet-landed runner bytes attest themselves.
2. Publish the full hardening as one exact descendant containing the bootstrap
   ancestry, obtain topic-push CI results, and treat CI only as an early
   cross-platform signal. Do not create a successor from CI alone.
3. After the bootstrap is production-adopted and at or after Aug 26 00:30, use
   repository-owned workload admission to run the
   exact-tip integration preflight and complete bounded suite in an isolated
   worktree. Preserve immutable qualification logs and verdicts. Any partial,
   timed-out, stale, or uncontained run is not PASS.
4. Only after qualification PASS, re-fetch and revalidate the live topic and
   master, canonical origin, production baseline, clean worktree, helper
   hashes, preparation evidence, and a complete future schedule. Then create
   and arm one exact successor for no earlier than Aug 27.
5. Require the successor to repeat preflight and the full suite for overnight
   drift, then require guarded merge recovery, documentation evidence,
   publication acknowledgement, and exact local/remote equality.
6. After the merge and documentation closeout, perform every fresh attended
   live-readiness gate. Stop before authenticated mutation if any gate is stale,
   absent, contradictory, or outside its supported window.

## Verification boundary

- Protected-window-safe independent static review is complete. No
  post-a2-failure Python import, pytest, compileall, bounded suite, successor
  Scheduler registration, credential access, network/exchange contact, or
  production mutation is part of the hardening branch's evidence.
- The process can reduce known failure classes and make unknown failures safe;
  it cannot promise that every unknowable defect will be predicted. The durable
  standard is early detection, immutable diagnosis, fail-closed containment,
  and no false claim of readiness.

## Update this file when
Rewrite after exact-tip publication/CI, admitted qualification, successor creation or
terminal result, production merge, credential comparison, a fresh live-readiness pass, or any Stage 0/1 result.
